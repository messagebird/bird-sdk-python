# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppGroup,
    WhatsAppGroupPinnedMessage,
    WhatsAppGroupPinnedMessageCreate,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class _WhatsappGroupsPinsCreateRequired(TypedDict):
    message_id: str


class WhatsappGroupsPinsCreateParams(_WhatsappGroupsPinsCreateRequired, total=False):
    """Params for ``client.whatsapp.groups.pins.create``. ``message_id`` is required."""

    duration_days: int


class WhatsappGroupsPins(Resource):
    def create(
        self,
        group_id: str,
        *,
        message_id: str,
        duration_days: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupPinnedMessage:
        """Pin one of the group's messages at the top of its chat for 1 to 30 days. A group holds 3 pinned messages; pinning a fourth unpins the oldest. The group's pinned_messages carries what is pinned.

        ```python
        pin = client.whatsapp.groups.pins.create(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            message_id="wam_01kya19eknftrs2s6p82asmvnh",
        )
        print(pin.pinned_until)
        ```
        """
        body = to_wire(
            WhatsAppGroupPinnedMessageCreate,
            {
                "message_id": message_id,
                "duration_days": duration_days,
            },
        )
        return self._write(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/pinned-messages",
            body,
            WhatsAppGroupPinnedMessage,
            options,
        )

    def delete(
        self,
        group_id: str,
        message_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppGroup:
        """Unpin one of the group's messages before its pin expires. Repeating the call is safe: a message this group carries that is not pinned changes nothing and still answers 200. A message from another group returns a 422 WhatsAppMessageNotInGroup.

        ```python
        group = client.whatsapp.groups.pins.delete(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            "wam_01kya19eknftrs2s6p82asmvnh",
        )
        print(len(group.pinned_messages or []))
        ```
        """
        return self._delete_model(f"/v1/whatsapp/groups/{quote(group_id, safe='')}/pinned-messages/{quote(message_id, safe='')}", WhatsAppGroup, options)


class AsyncWhatsappGroupsPins(AsyncResource):
    async def create(
        self,
        group_id: str,
        *,
        message_id: str,
        duration_days: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupPinnedMessage:
        """Pin one of the group's messages at the top of its chat for 1 to 30 days. A group holds 3 pinned messages; pinning a fourth unpins the oldest. The group's pinned_messages carries what is pinned.

        ```python
        pin = await client.whatsapp.groups.pins.create(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            message_id="wam_01kya19eknftrs2s6p82asmvnh",
        )
        print(pin.pinned_until)
        ```
        """
        body = to_wire(
            WhatsAppGroupPinnedMessageCreate,
            {
                "message_id": message_id,
                "duration_days": duration_days,
            },
        )
        return await self._write(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/pinned-messages",
            body,
            WhatsAppGroupPinnedMessage,
            options,
        )

    async def delete(
        self,
        group_id: str,
        message_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppGroup:
        """Unpin one of the group's messages before its pin expires. Repeating the call is safe: a message this group carries that is not pinned changes nothing and still answers 200. A message from another group returns a 422 WhatsAppMessageNotInGroup.

        ```python
        group = await client.whatsapp.groups.pins.delete(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            "wam_01kya19eknftrs2s6p82asmvnh",
        )
        print(len(group.pinned_messages or []))
        ```
        """
        return await self._delete_model(f"/v1/whatsapp/groups/{quote(group_id, safe='')}/pinned-messages/{quote(message_id, safe='')}", WhatsAppGroup, options)
