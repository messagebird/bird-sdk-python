# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from urllib.parse import quote

from bird._generated import (
    SMSTemplateLanguage,
    SMSTemplateLanguageList,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class SmsTemplatesVersionsLanguages(Resource):
    def list(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSTemplateLanguageList:
        """List a version's languages without their text. Each summary includes its revision, content hash, and last update time. Read a language item to retrieve its text.

        ```python
        languages = client.sms_templates.versions.languages.list(
            "bird_otp_verification", "smv_01ky4x8e4genzb7way45txfkm1"
        )
        for language in languages.data:
            print(language.language, language.revision)
        ```
        """
        return self._get(
            f"/v1/sms/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages",
            {},
            SMSTemplateLanguageList,
            options,
        )

    def get(
        self,
        template_ref: str,
        version_id: str,
        language: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSTemplateLanguage:
        """Read one language's full text, revision, content hash, and update time from a draft, published version, or built-in template's synthetic version.

        ```python
        language = client.sms_templates.versions.languages.get(
            "bird_otp_verification", "smv_01ky4x8e4genzb7way45txfkm1", "en"
        )
        print(language.language, language.text)
        ```
        """
        return self._get(
            f"/v1/sms/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages/{quote(language, safe='')}",
            {},
            SMSTemplateLanguage,
            options,
        )


class AsyncSmsTemplatesVersionsLanguages(AsyncResource):
    async def list(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSTemplateLanguageList:
        """List a version's languages without their text. Each summary includes its revision, content hash, and last update time. Read a language item to retrieve its text.

        ```python
        languages = await client.sms_templates.versions.languages.list(
            "bird_otp_verification", "smv_01ky4x8e4genzb7way45txfkm1"
        )
        for language in languages.data:
            print(language.language, language.revision)
        ```
        """
        return await self._get(
            f"/v1/sms/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages",
            {},
            SMSTemplateLanguageList,
            options,
        )

    async def get(
        self,
        template_ref: str,
        version_id: str,
        language: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSTemplateLanguage:
        """Read one language's full text, revision, content hash, and update time from a draft, published version, or built-in template's synthetic version.

        ```python
        language = await client.sms_templates.versions.languages.get(
            "bird_otp_verification", "smv_01ky4x8e4genzb7way45txfkm1", "en"
        )
        print(language.language, language.text)
        ```
        """
        return await self._get(
            f"/v1/sms/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages/{quote(language, safe='')}",
            {},
            SMSTemplateLanguage,
            options,
        )
