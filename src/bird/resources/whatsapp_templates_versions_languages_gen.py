# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from urllib.parse import quote

from bird._generated import (
    WhatsAppTemplateLanguage,
    WhatsAppTemplateLanguageList,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class WhatsappTemplatesVersionsLanguages(Resource):
    def list(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplateLanguageList:
        """List the languages one WhatsApp template version carries, with each language's review outcome at Meta and a content hash to tell which ones actually differ before fetching any of them. Use `whatsapp.templates.versions.languages.get` for a single language's content blocks.

        ```python
        languages = client.whatsapp.templates.versions.languages.list(
            "bird_otp", "wav_01ky4x8e4genzb7way45txfkm1"
        )
        for language in languages.data:
            print(language.language, language.status)
        ```
        """
        return self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages",
            {},
            WhatsAppTemplateLanguageList,
            options,
        )

    def get(
        self,
        template_ref: str,
        version_id: str,
        language: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplateLanguage:
        """Get one language of one WhatsApp template version: its content blocks, what this version's submission did with it, and Meta's review outcome and category for it.

        ```python
        language = client.whatsapp.templates.versions.languages.get(
            "bird_otp", "wav_01ky4x8e4genzb7way45txfkm1", "nl-BE"
        )
        for component in language.components:
            print(component.type)
        ```
        """
        return self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages/{quote(language, safe='')}",
            {},
            WhatsAppTemplateLanguage,
            options,
        )


class AsyncWhatsappTemplatesVersionsLanguages(AsyncResource):
    async def list(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplateLanguageList:
        """List the languages one WhatsApp template version carries, with each language's review outcome at Meta and a content hash to tell which ones actually differ before fetching any of them. Use `whatsapp.templates.versions.languages.get` for a single language's content blocks.

        ```python
        languages = await client.whatsapp.templates.versions.languages.list(
            "bird_otp", "wav_01ky4x8e4genzb7way45txfkm1"
        )
        for language in languages.data:
            print(language.language, language.status)
        ```
        """
        return await self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages",
            {},
            WhatsAppTemplateLanguageList,
            options,
        )

    async def get(
        self,
        template_ref: str,
        version_id: str,
        language: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplateLanguage:
        """Get one language of one WhatsApp template version: its content blocks, what this version's submission did with it, and Meta's review outcome and category for it.

        ```python
        language = await client.whatsapp.templates.versions.languages.get(
            "bird_otp", "wav_01ky4x8e4genzb7way45txfkm1", "nl-BE"
        )
        for component in language.components:
            print(component.type)
        ```
        """
        return await self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}/languages/{quote(language, safe='')}",
            {},
            WhatsAppTemplateLanguage,
            options,
        )
