# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from collections.abc import Sequence
from typing import Any, TypedDict
from urllib.parse import quote

from bird._generated import (
    SMSKeywordRule,
    SMSKeywordRuleCreate,
    SMSKeywordRuleList,
    SMSKeywordRuleUpdate,
)
from bird._models import to_wire_exclude_unset
from bird._resource import AsyncResource, Resource
from bird._types import Omit, RequestOptions, omit


class SmsKeywordRulesListParams(TypedDict, total=False):
    """Query params for ``client.sms_keyword_rules.list``. Every key is optional."""

    country: str
    number: str
    from_country: str
    operation: str
    scope: str


class _SmsKeywordRulesCreateRequired(TypedDict):
    operation: str


class SmsKeywordRulesCreateParams(_SmsKeywordRulesCreateRequired, total=False):
    """Params for ``client.sms_keyword_rules.create``. ``operation`` is required."""

    country: str | None
    language: str | None
    number: str | None
    keywords: Sequence[str]
    reply: str | None
    confirmed_self_managed: bool


class SmsKeywordRulesUpdateParams(TypedDict, total=False):
    """Params for ``client.sms_keyword_rules.update``. Every key is optional."""

    keywords: Sequence[str]
    reply: str | None
    confirmed_self_managed: bool


class SmsKeywordRules(Resource):
    def list(
        self,
        *,
        country: str | None = None,
        number: str | None = None,
        from_country: str | None = None,
        operation: str | None = None,
        scope: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRuleList:
        """List the default and workspace keyword rules that apply to inbound messages, most specific first. Filter by `country`, `number`, `operation`, or `scope`. Pass `number` to see one number's rules in evaluation order. Default coverage varies by country."""
        return self._get(
            "/v1/sms/keyword-rules",
            {
                "country": country,
                "number": number,
                "from_country": from_country,
                "operation": operation,
                "scope": scope,
            },
            SMSKeywordRuleList,
            options,
        )

    def get(
        self,
        id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRule:
        """Read one default or workspace keyword rule. Its ID prefix identifies which kind: a workspace rule can be changed with `sms_keyword_rules.update`, a Bird default cannot."""
        return self._get(
            f"/v1/sms/keyword-rules/{quote(id, safe='')}",
            {},
            SMSKeywordRule,
            options,
        )

    def create(
        self,
        *,
        operation: str,
        country: str | None | Omit = omit,
        language: str | None | Omit = omit,
        number: str | None | Omit = omit,
        keywords: Sequence[str] | None = None,
        reply: str | None | Omit = omit,
        confirmed_self_managed: bool | None = None,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRule:
        """Replace the default opt-out, opt-in, or help reply for one country, or add a `custom` keyword. A workspace rule takes precedence over the default for that country. Opt-out and opt-in keywords cannot be assigned to another operation."""
        _body: dict[str, Any] = {}
        _body["operation"] = operation
        if not isinstance(country, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["country"] = country
        if not isinstance(language, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["language"] = language
        if not isinstance(number, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["number"] = number
        if keywords is not None:
            _body["keywords"] = keywords
        if not isinstance(reply, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["reply"] = reply
        if confirmed_self_managed is not None:
            _body["confirmed_self_managed"] = confirmed_self_managed
        body = to_wire_exclude_unset(SMSKeywordRuleCreate, _body)
        return self._write(
            "POST",
            "/v1/sms/keyword-rules",
            body,
            SMSKeywordRule,
            options,
        )

    def update(
        self,
        id: str,
        *,
        keywords: Sequence[str] | None = None,
        reply: str | None | Omit = omit,
        confirmed_self_managed: bool | None = None,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRule:
        """Change one of your own keyword rules: its reply, its extra keywords, or its self-managed attestation. Bird's default rules cannot be updated; create your own for that country instead with `sms_keyword_rules.create`. Omitting `keywords` leaves the set alone, while sending an empty list clears your additions back to Bird's."""
        _body: dict[str, Any] = {}
        if keywords is not None:
            _body["keywords"] = keywords
        if not isinstance(reply, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["reply"] = reply
        if confirmed_self_managed is not None:
            _body["confirmed_self_managed"] = confirmed_self_managed
        body = to_wire_exclude_unset(SMSKeywordRuleUpdate, _body)
        return self._write(
            "PATCH",
            f"/v1/sms/keyword-rules/{quote(id, safe='')}",
            body,
            SMSKeywordRule,
            options,
        )

    def delete(
        self,
        id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Delete one of your own keyword rules, which restores Bird's default for that country and operation. Bird's own rules cannot be deleted."""
        self._delete(f"/v1/sms/keyword-rules/{quote(id, safe='')}", options)


class AsyncSmsKeywordRules(AsyncResource):
    async def list(
        self,
        *,
        country: str | None = None,
        number: str | None = None,
        from_country: str | None = None,
        operation: str | None = None,
        scope: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRuleList:
        """List the default and workspace keyword rules that apply to inbound messages, most specific first. Filter by `country`, `number`, `operation`, or `scope`. Pass `number` to see one number's rules in evaluation order. Default coverage varies by country."""
        return await self._get(
            "/v1/sms/keyword-rules",
            {
                "country": country,
                "number": number,
                "from_country": from_country,
                "operation": operation,
                "scope": scope,
            },
            SMSKeywordRuleList,
            options,
        )

    async def get(
        self,
        id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRule:
        """Read one default or workspace keyword rule. Its ID prefix identifies which kind: a workspace rule can be changed with `sms_keyword_rules.update`, a Bird default cannot."""
        return await self._get(
            f"/v1/sms/keyword-rules/{quote(id, safe='')}",
            {},
            SMSKeywordRule,
            options,
        )

    async def create(
        self,
        *,
        operation: str,
        country: str | None | Omit = omit,
        language: str | None | Omit = omit,
        number: str | None | Omit = omit,
        keywords: Sequence[str] | None = None,
        reply: str | None | Omit = omit,
        confirmed_self_managed: bool | None = None,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRule:
        """Replace the default opt-out, opt-in, or help reply for one country, or add a `custom` keyword. A workspace rule takes precedence over the default for that country. Opt-out and opt-in keywords cannot be assigned to another operation."""
        _body: dict[str, Any] = {}
        _body["operation"] = operation
        if not isinstance(country, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["country"] = country
        if not isinstance(language, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["language"] = language
        if not isinstance(number, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["number"] = number
        if keywords is not None:
            _body["keywords"] = keywords
        if not isinstance(reply, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["reply"] = reply
        if confirmed_self_managed is not None:
            _body["confirmed_self_managed"] = confirmed_self_managed
        body = to_wire_exclude_unset(SMSKeywordRuleCreate, _body)
        return await self._write(
            "POST",
            "/v1/sms/keyword-rules",
            body,
            SMSKeywordRule,
            options,
        )

    async def update(
        self,
        id: str,
        *,
        keywords: Sequence[str] | None = None,
        reply: str | None | Omit = omit,
        confirmed_self_managed: bool | None = None,
        options: RequestOptions | None = None,
    ) -> SMSKeywordRule:
        """Change one of your own keyword rules: its reply, its extra keywords, or its self-managed attestation. Bird's default rules cannot be updated; create your own for that country instead with `sms_keyword_rules.create`. Omitting `keywords` leaves the set alone, while sending an empty list clears your additions back to Bird's."""
        _body: dict[str, Any] = {}
        if keywords is not None:
            _body["keywords"] = keywords
        if not isinstance(reply, Omit):  # None clears (JSON null); omit leaves it unchanged
            _body["reply"] = reply
        if confirmed_self_managed is not None:
            _body["confirmed_self_managed"] = confirmed_self_managed
        body = to_wire_exclude_unset(SMSKeywordRuleUpdate, _body)
        return await self._write(
            "PATCH",
            f"/v1/sms/keyword-rules/{quote(id, safe='')}",
            body,
            SMSKeywordRule,
            options,
        )

    async def delete(
        self,
        id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Delete one of your own keyword rules, which restores Bird's default for that country and operation. Bird's own rules cannot be deleted."""
        await self._delete(f"/v1/sms/keyword-rules/{quote(id, safe='')}", options)
