# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppTemplateVersion,
    WhatsAppTemplateVersionSummary,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class WhatsappTemplatesVersionsListParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.templates.versions.list``. Every key is optional."""

    limit: int
    starting_after: str
    ending_before: str


class WhatsappTemplatesVersionsBase(Resource):
    def list(
        self,
        template_ref: str,
        *,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[WhatsAppTemplateVersionSummary]:
        """List a WhatsApp template's versions, newest first. Each version is one immutable submission; an open draft, if one exists, appears alongside them. Use this to find the version a send resolves to, or the draft still being edited.

        ```python
        for version in client.whatsapp.templates.versions.list("bird_otp"):
            print(version.id, version.version_number)
        ```
        """
        query = {
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions", query, WhatsAppTemplateVersionSummary, options)

    def get(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplateVersion:
        """Get one version of a WhatsApp template: its submission state and the languages it carries. This is the level at which content exists.

        ```python
        version = client.whatsapp.templates.versions.get("bird_otp", "wav_01ky4x8e4genzb7way45txfkm1")
        print(version.id, list(version.languages))
        ```
        """
        return self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}",
            {},
            WhatsAppTemplateVersion,
            options,
        )


class AsyncWhatsappTemplatesVersionsBase(AsyncResource):
    def list(
        self,
        template_ref: str,
        *,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[WhatsAppTemplateVersionSummary]:
        """List a WhatsApp template's versions, newest first. Each version is one immutable submission; an open draft, if one exists, appears alongside them. Use this to find the version a send resolves to, or the draft still being edited.

        ```python
        async for version in client.whatsapp.templates.versions.list("bird_otp"):
            print(version.id, version.version_number)
        ```
        """
        query = {
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions", query, WhatsAppTemplateVersionSummary, options)

    async def get(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplateVersion:
        """Get one version of a WhatsApp template: its submission state and the languages it carries. This is the level at which content exists.

        ```python
        version = await client.whatsapp.templates.versions.get("bird_otp", "wav_01ky4x8e4genzb7way45txfkm1")
        print(version.id, list(version.languages))
        ```
        """
        return await self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}",
            {},
            WhatsAppTemplateVersion,
            options,
        )
