# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppReactionAccepted,
    WhatsAppReactionEvent,
    WhatsAppReactionUpsert,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class _WhatsappReactionSetRequired(TypedDict):
    emoji: str


class WhatsappReactionSetParams(_WhatsappReactionSetRequired, total=False):
    """Params for ``client.whatsapp.reaction.set``. ``emoji`` is required."""


class WhatsappReactionListEventsParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.reaction.list_events``. Every key is optional."""

    limit: int
    starting_after: str
    ending_before: str


class WhatsappReaction(Resource):
    def set(
        self,
        message_id: str,
        *,
        emoji: str,
        options: RequestOptions | None = None,
    ) -> WhatsAppReactionAccepted:
        """Place an emoji reaction on a WhatsApp message the workspace received. You hold at most one reaction per message, so calling this again replaces the emoji rather than adding a second one. The message must be one the contact sent and still resolvable: Bird keeps the id a reaction needs for 15 days, so an older message is a 404. The 202 is the reaction accepted, not delivered: use whatsapp_get and read the message's `reactions` to see what stands, or whatsapp_reaction_list_events for what WhatsApp made of it.

        ```python
        reaction = client.whatsapp.reaction.set("wam_01krdgeqcxet5s7t44vh8rt9mg", emoji="\U0001f44d")
        print(reaction.id, reaction.emoji)
        ```
        """
        body = to_wire(
            WhatsAppReactionUpsert,
            {
                "emoji": emoji,
            },
        )
        return self._write(
            "PUT",
            f"/v1/whatsapp/messages/{quote(message_id, safe='')}/reaction",
            body,
            WhatsAppReactionAccepted,
            options,
        )

    def remove(
        self,
        message_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Take back the reaction this workspace placed on a WhatsApp message. Removing a reaction from a message you have not reacted to changes nothing and still answers 202. A removal is refused on the same grounds as a placement: a message this workspace sent is a 422, and one Bird can no longer resolve is a 404, on the same 15-day retention. The 202 is the removal accepted, not applied: the reaction leaves the message's `reactions`, readable with whatsapp_get, once WhatsApp confirms it, and whatsapp_reaction_list_events records the removal either way.

        ```python
        client.whatsapp.reaction.remove("wam_01krdgeqcxet5s7t44vh8rt9mg")
        ```
        """
        self._delete(f"/v1/whatsapp/messages/{quote(message_id, safe='')}/reaction", options)

    def list_events(
        self,
        message_id: str,
        *,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[WhatsAppReactionEvent]:
        """Get every change made to one WhatsApp message's reactions, newest first, as a cursor page ({data, next_cursor, …}): each emoji placed, each one replaced by another, and each one taken back, by the contact and by this workspace alike. Entries carry a status, so a reaction WhatsApp refused is visible here with its error. Use whatsapp_get and read the message's `reactions` instead for what currently stands.

        ```python
        for event in client.whatsapp.reaction.list_events("wam_01krdgeqcxet5s7t44vh8rt9mg"):
            print(event.id, event.emoji, event.status)
        ```
        """
        query = {
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, f"/v1/whatsapp/messages/{quote(message_id, safe='')}/reaction-events", query, WhatsAppReactionEvent, options)


class AsyncWhatsappReaction(AsyncResource):
    async def set(
        self,
        message_id: str,
        *,
        emoji: str,
        options: RequestOptions | None = None,
    ) -> WhatsAppReactionAccepted:
        """Place an emoji reaction on a WhatsApp message the workspace received. You hold at most one reaction per message, so calling this again replaces the emoji rather than adding a second one. The message must be one the contact sent and still resolvable: Bird keeps the id a reaction needs for 15 days, so an older message is a 404. The 202 is the reaction accepted, not delivered: use whatsapp_get and read the message's `reactions` to see what stands, or whatsapp_reaction_list_events for what WhatsApp made of it.

        ```python
        reaction = await client.whatsapp.reaction.set("wam_01krdgeqcxet5s7t44vh8rt9mg", emoji="\U0001f44d")
        print(reaction.id, reaction.emoji)
        ```
        """
        body = to_wire(
            WhatsAppReactionUpsert,
            {
                "emoji": emoji,
            },
        )
        return await self._write(
            "PUT",
            f"/v1/whatsapp/messages/{quote(message_id, safe='')}/reaction",
            body,
            WhatsAppReactionAccepted,
            options,
        )

    async def remove(
        self,
        message_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Take back the reaction this workspace placed on a WhatsApp message. Removing a reaction from a message you have not reacted to changes nothing and still answers 202. A removal is refused on the same grounds as a placement: a message this workspace sent is a 422, and one Bird can no longer resolve is a 404, on the same 15-day retention. The 202 is the removal accepted, not applied: the reaction leaves the message's `reactions`, readable with whatsapp_get, once WhatsApp confirms it, and whatsapp_reaction_list_events records the removal either way.

        ```python
        await client.whatsapp.reaction.remove("wam_01krdgeqcxet5s7t44vh8rt9mg")
        ```
        """
        await self._delete(f"/v1/whatsapp/messages/{quote(message_id, safe='')}/reaction", options)

    def list_events(
        self,
        message_id: str,
        *,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[WhatsAppReactionEvent]:
        """Get every change made to one WhatsApp message's reactions, newest first, as a cursor page ({data, next_cursor, …}): each emoji placed, each one replaced by another, and each one taken back, by the contact and by this workspace alike. Entries carry a status, so a reaction WhatsApp refused is visible here with its error. Use whatsapp_get and read the message's `reactions` instead for what currently stands.

        ```python
        async for event in client.whatsapp.reaction.list_events("wam_01krdgeqcxet5s7t44vh8rt9mg"):
            print(event.id, event.emoji, event.status)
        ```
        """
        query = {
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, f"/v1/whatsapp/messages/{quote(message_id, safe='')}/reaction-events", query, WhatsAppReactionEvent, options)
