# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict

from bird._generated import (
    SMSInboundStatsByCountryResponse,
    SMSInboundStatsByNumberResponse,
    SMSInboundStatsByOperatorResponse,
    SMSInboundStatsResponse,
    SMSInboundStatsSummaryResponse,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class SmsStatsInboundSummaryParams(TypedDict, total=False):
    """Query params for ``client.sms.stats.inbound.summary``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    compare: str


class SmsStatsInboundDailyParams(TypedDict, total=False):
    """Query params for ``client.sms.stats.inbound.daily``. Every key is optional."""

    from_: str
    to: str
    timezone: str


class SmsStatsInboundHourlyParams(TypedDict, total=False):
    """Query params for ``client.sms.stats.inbound.hourly``. Every key is optional."""

    from_: str
    to: str
    timezone: str


class SmsStatsInboundByCountryParams(TypedDict, total=False):
    """Query params for ``client.sms.stats.inbound.by_country``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class SmsStatsInboundByOperatorParams(TypedDict, total=False):
    """Query params for ``client.sms.stats.inbound.by_operator``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class SmsStatsInboundByNumberParams(TypedDict, total=False):
    """Query params for ``client.sms.stats.inbound.by_number``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class SmsStatsInbound(Resource):
    def summary(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        compare: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsSummaryResponse:
        """Total messages your numbers received over a period. For a breakdown use `sms.stats.inbound.by_country`, `sms.stats.inbound.by_operator`, or `sms.stats.inbound.by_number`.

        ```python
        summary = client.sms.stats.inbound.summary(from_="2026-05-01", to="2026-05-31")
        print(summary.received)
        ```
        """
        return self._get(
            "/v1/sms/stats/inbound/summary",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "compare": compare,
            },
            SMSInboundStatsSummaryResponse,
            options,
        )

    def daily(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsResponse:
        """Messages your numbers received, one row per calendar day. Set `timezone` to get local calendar days instead of UTC.

        ```python
        stats = client.sms.stats.inbound.daily(from_="2026-05-01", to="2026-05-31")
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return self._get(
            "/v1/sms/stats/inbound/daily",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            SMSInboundStatsResponse,
            options,
        )

    def hourly(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsResponse:
        """Messages your numbers received, one row per hour, for inspecting inbound volume inside a single day.

        ```python
        stats = client.sms.stats.inbound.hourly(
            from_="2026-05-30T00:00:00Z", to="2026-05-31T00:00:00Z"
        )
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return self._get(
            "/v1/sms/stats/inbound/hourly",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            SMSInboundStatsResponse,
            options,
        )

    def by_country(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsByCountryResponse:
        """Messages your numbers received, grouped by the country of the receiving number.

        ```python
        stats = client.sms.stats.inbound.by_country(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            print(row.country, row.received)
        ```
        """
        return self._get(
            "/v1/sms/stats/inbound/countries",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            SMSInboundStatsByCountryResponse,
            options,
        )

    def by_operator(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsByOperatorResponse:
        """Messages your numbers received, grouped by the sender's mobile operator. Messages whose operator the carrier did not report are excluded, so these rows can sum to less than `sms.stats.inbound.summary` for the same period.

        ```python
        stats = client.sms.stats.inbound.by_operator(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            # Messages whose operator the carrier did not report are excluded, so these
            # rows can sum to less than the inbound summary for the same period.
            print(row.mcc_mnc, row.received)
        ```
        """
        return self._get(
            "/v1/sms/stats/inbound/operators",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            SMSInboundStatsByOperatorResponse,
            options,
        )

    def by_number(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsByNumberResponse:
        """How many messages each of your numbers received, which is the view that shows whether a campaign's reply traffic is landing on the number you expect.

        ```python
        stats = client.sms.stats.inbound.by_number(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            print(row.number, row.received)
        ```
        """
        return self._get(
            "/v1/sms/stats/inbound/numbers",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            SMSInboundStatsByNumberResponse,
            options,
        )


class AsyncSmsStatsInbound(AsyncResource):
    async def summary(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        compare: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsSummaryResponse:
        """Total messages your numbers received over a period. For a breakdown use `sms.stats.inbound.by_country`, `sms.stats.inbound.by_operator`, or `sms.stats.inbound.by_number`.

        ```python
        summary = await client.sms.stats.inbound.summary(from_="2026-05-01", to="2026-05-31")
        print(summary.received)
        ```
        """
        return await self._get(
            "/v1/sms/stats/inbound/summary",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "compare": compare,
            },
            SMSInboundStatsSummaryResponse,
            options,
        )

    async def daily(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsResponse:
        """Messages your numbers received, one row per calendar day. Set `timezone` to get local calendar days instead of UTC.

        ```python
        stats = await client.sms.stats.inbound.daily(from_="2026-05-01", to="2026-05-31")
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return await self._get(
            "/v1/sms/stats/inbound/daily",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            SMSInboundStatsResponse,
            options,
        )

    async def hourly(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsResponse:
        """Messages your numbers received, one row per hour, for inspecting inbound volume inside a single day.

        ```python
        stats = await client.sms.stats.inbound.hourly(
            from_="2026-05-30T00:00:00Z", to="2026-05-31T00:00:00Z"
        )
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return await self._get(
            "/v1/sms/stats/inbound/hourly",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            SMSInboundStatsResponse,
            options,
        )

    async def by_country(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsByCountryResponse:
        """Messages your numbers received, grouped by the country of the receiving number.

        ```python
        stats = await client.sms.stats.inbound.by_country(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            print(row.country, row.received)
        ```
        """
        return await self._get(
            "/v1/sms/stats/inbound/countries",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            SMSInboundStatsByCountryResponse,
            options,
        )

    async def by_operator(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsByOperatorResponse:
        """Messages your numbers received, grouped by the sender's mobile operator. Messages whose operator the carrier did not report are excluded, so these rows can sum to less than `sms.stats.inbound.summary` for the same period.

        ```python
        stats = await client.sms.stats.inbound.by_operator(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            # Messages whose operator the carrier did not report are excluded, so these
            # rows can sum to less than the inbound summary for the same period.
            print(row.mcc_mnc, row.received)
        ```
        """
        return await self._get(
            "/v1/sms/stats/inbound/operators",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            SMSInboundStatsByOperatorResponse,
            options,
        )

    async def by_number(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> SMSInboundStatsByNumberResponse:
        """How many messages each of your numbers received, which is the view that shows whether a campaign's reply traffic is landing on the number you expect.

        ```python
        stats = await client.sms.stats.inbound.by_number(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            print(row.number, row.received)
        ```
        """
        return await self._get(
            "/v1/sms/stats/inbound/numbers",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            SMSInboundStatsByNumberResponse,
            options,
        )
