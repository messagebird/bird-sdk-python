# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict

from bird._generated import (
    WhatsAppStatsByCountryResponse,
    WhatsAppStatsByErrorCodeResponse,
    WhatsAppStatsByPhoneNumberResponse,
    WhatsAppStatsByTagResponse,
    WhatsAppStatsByTemplateCategoryResponse,
    WhatsAppStatsByTemplateResponse,
    WhatsAppStatsResponse,
    WhatsAppStatsSummary,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class WhatsappStatsSummaryParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.summary``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    template: str
    category: str
    phone_number: str
    tag: str
    compare: str


class WhatsappStatsDailyParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.daily``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    template: str
    category: str
    phone_number: str
    tag: str


class WhatsappStatsHourlyParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.hourly``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    template: str
    category: str
    phone_number: str
    tag: str


class WhatsappStatsByErrorCodeParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.by_error_code``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsByTemplateParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.by_template``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsByTemplateCategoryParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.by_template_category``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsByTagParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.by_tag``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsByPhoneNumberParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.by_phone_number``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsByCountryParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.by_country``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsBase(Resource):
    def summary(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        template: str | None = None,
        category: str | None = None,
        phone_number: str | None = None,
        tag: str | None = None,
        compare: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsSummary:
        """Aggregate outbound WhatsApp counts for one window: accepted, sent, delivered, failed and rejected, with delivery and failure rates, read engagement, and processing and end-to-end latency percentiles. Restrict it to one dimension with `template`, `category`, `phone_number` or `tag`, one at a time, not combined. Set `compare` to `previous_period` for the deltas against the window immediately before. `from` and `to` are optional and default to the trailing 30 days. Counts are attributed to the time each message was accepted, so a delivery confirmed today counts against the day its message was accepted; `period.data_as_of` is the freshness boundary the rollups have reached.

        ```python
        summary = client.whatsapp.stats.summary(
            from_="2026-08-01", to="2026-08-31", timezone="Europe/Amsterdam"
        )
        print(summary.delivery, summary.latency)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/summary",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "template": template,
                "category": category,
                "phone_number": phone_number,
                "tag": tag,
                "compare": compare,
            },
            WhatsAppStatsSummary,
            options,
        )

    def daily(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        template: str | None = None,
        category: str | None = None,
        phone_number: str | None = None,
        tag: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsResponse:
        """Outbound WhatsApp lifecycle counts, one row per calendar day of the window, gap-filled so a silent day is a zero row rather than a missing one. Every event a message produces counts against the day that message was accepted, so a row is a funnel over that day's sends and recent rows under-report while callbacks arrive. Each row carries its own latency percentiles, but delivery, failure and read rates are whole-window figures: read those from `whatsapp.stats.summary`. Accepts the same single-dimension restriction: `template`, `category`, `phone_number` or `tag`. Days are calendar days in `timezone`, or UTC when it is omitted. Max window 365 days; `from` and `to` default to the trailing 30 days.

        ```python
        stats = client.whatsapp.stats.daily(from_="2026-08-01", to="2026-08-31")
        for point in stats.data or []:
            print(point.bucket, point.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/daily",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "template": template,
                "category": category,
                "phone_number": phone_number,
                "tag": tag,
            },
            WhatsAppStatsResponse,
            options,
        )

    def hourly(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        template: str | None = None,
        category: str | None = None,
        phone_number: str | None = None,
        tag: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsResponse:
        """Outbound WhatsApp lifecycle counts, one row per hour, gap-filled. Every event a message produces counts against the hour that message was accepted, so a row is a funnel over that hour's sends and recent rows under-report while callbacks arrive. Each row carries its own latency percentiles, but delivery, failure and read rates are whole-window figures: read those from `whatsapp.stats.summary`. Use it where the daily grain hides the shape; a window spans at most 720 hours (30 days), and for anything longer use `whatsapp.stats.daily`. Accepts the same single-dimension restriction: `template`, `category`, `phone_number` or `tag`. `from` and `to` are optional RFC 3339 instants, defaulting to the trailing 168 hours.

        ```python
        stats = client.whatsapp.stats.hourly(from_="2026-08-30T00:00:00Z", to="2026-08-31T00:00:00Z")
        for point in stats.data or []:
            print(point.bucket, point.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/hourly",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "template": template,
                "category": category,
                "phone_number": phone_number,
                "tag": tag,
            },
            WhatsAppStatsResponse,
            options,
        )

    def by_error_code(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByErrorCodeResponse:
        """Failed outbound WhatsApp messages grouped by the normalized failure reason, the same value a failed message reports in `last_error.code`, ranked by volume. This is the read that answers "why are sends failing"; `whatsapp.stats.summary` gives the failure rate, this gives its composition. A send declined before handoff (insufficient balance, missing price, suppressed recipient) counts as rejected rather than failed and is absent here; read `delivery.rejected` on `whatsapp.stats.summary` for it. Rows are the whole workspace's: the breakdown takes no dimension filter. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = client.whatsapp.stats.by_error_code(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.error_code, row.count)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/error-codes",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByErrorCodeResponse,
            options,
        )

    def by_template(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByTemplateResponse:
        """Outbound WhatsApp counts grouped by template, keyed by `template_id`, ranked by accepted volume. Resolve the id through the WhatsApp template endpoints for a name. Rows sum to less than the period's accepted total: a free-form send resolves no template, and some volume cannot be attributed to one at all, so take the denominator from `whatsapp.stats.summary`. Rows are the whole workspace's: the breakdown takes no dimension filter; to see one template over time, filter `whatsapp.stats.daily` by `template` instead. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = client.whatsapp.stats.by_template(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.template_id, row.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/templates",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByTemplateResponse,
            options,
        )

    def by_template_category(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByTemplateCategoryResponse:
        """Outbound WhatsApp counts grouped by Meta's template category (`authentication`, `utility` and `marketing` today, and the vocabulary is open), ranked by accepted volume. Category is what WhatsApp prices a message on, so this is the breakdown that explains a spend change. A free-form send carries no category, so rows sum to less than the period's accepted total; take the denominator from `whatsapp.stats.summary`. Rows are the whole workspace's: the breakdown takes no dimension filter. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = client.whatsapp.stats.by_template_category(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.category, row.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/template-categories",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByTemplateCategoryResponse,
            options,
        )

    def by_tag(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByTagResponse:
        """Outbound WhatsApp counts grouped by the tags a send attached, ranked by accepted volume. A row's tag is `name:value`, so one tag name spreads over as many rows as it has values. Rows are the whole workspace's: the breakdown takes no dimension filter; to see one tag over time, filter `whatsapp.stats.daily` by `tag` instead. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = client.whatsapp.stats.by_tag(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.tag, row.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/tags",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByTagResponse,
            options,
        )

    def by_phone_number(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByPhoneNumberResponse:
        """Outbound WhatsApp counts grouped by the business number that sent them, ranked by accepted volume, each row marked as a number your workspace connected or one Bird manages on your behalf. Use it to find the sender behind a delivery problem: a row identifies the number in E.164 form. Rows are the whole workspace's: the breakdown takes no dimension filter.

        ```python
        stats = client.whatsapp.stats.by_phone_number(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.phone_number, row.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/phone-numbers",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByPhoneNumberResponse,
            options,
        )

    def by_country(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByCountryResponse:
        """Outbound WhatsApp delivery, read and latency stats grouped by the recipient's country, ranked by accepted volume. Use it to compare markets. A weak market cannot be narrowed further: no breakdown takes a country filter, so `whatsapp.stats.by_error_code` reports the workspace's failure reasons rather than the selected country's, and pairing the two is inference, not attribution. A recipient whose country cannot be resolved is counted under `ZZ`, so one-to-one sends always land somewhere. Group sends are the exception and are absent by design: one spans up to eight countries and has no single destination, so rows sum to `whatsapp.stats.summary` less that group volume. Do not report the difference as missing data. Rows are the whole workspace's: the breakdown takes no dimension filter.

        ```python
        stats = client.whatsapp.stats.by_country(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.country, row.delivery)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/countries",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByCountryResponse,
            options,
        )


class AsyncWhatsappStatsBase(AsyncResource):
    async def summary(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        template: str | None = None,
        category: str | None = None,
        phone_number: str | None = None,
        tag: str | None = None,
        compare: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsSummary:
        """Aggregate outbound WhatsApp counts for one window: accepted, sent, delivered, failed and rejected, with delivery and failure rates, read engagement, and processing and end-to-end latency percentiles. Restrict it to one dimension with `template`, `category`, `phone_number` or `tag`, one at a time, not combined. Set `compare` to `previous_period` for the deltas against the window immediately before. `from` and `to` are optional and default to the trailing 30 days. Counts are attributed to the time each message was accepted, so a delivery confirmed today counts against the day its message was accepted; `period.data_as_of` is the freshness boundary the rollups have reached.

        ```python
        summary = await client.whatsapp.stats.summary(
            from_="2026-08-01", to="2026-08-31", timezone="Europe/Amsterdam"
        )
        print(summary.delivery, summary.latency)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/summary",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "template": template,
                "category": category,
                "phone_number": phone_number,
                "tag": tag,
                "compare": compare,
            },
            WhatsAppStatsSummary,
            options,
        )

    async def daily(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        template: str | None = None,
        category: str | None = None,
        phone_number: str | None = None,
        tag: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsResponse:
        """Outbound WhatsApp lifecycle counts, one row per calendar day of the window, gap-filled so a silent day is a zero row rather than a missing one. Every event a message produces counts against the day that message was accepted, so a row is a funnel over that day's sends and recent rows under-report while callbacks arrive. Each row carries its own latency percentiles, but delivery, failure and read rates are whole-window figures: read those from `whatsapp.stats.summary`. Accepts the same single-dimension restriction: `template`, `category`, `phone_number` or `tag`. Days are calendar days in `timezone`, or UTC when it is omitted. Max window 365 days; `from` and `to` default to the trailing 30 days.

        ```python
        stats = await client.whatsapp.stats.daily(from_="2026-08-01", to="2026-08-31")
        for point in stats.data or []:
            print(point.bucket, point.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/daily",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "template": template,
                "category": category,
                "phone_number": phone_number,
                "tag": tag,
            },
            WhatsAppStatsResponse,
            options,
        )

    async def hourly(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        template: str | None = None,
        category: str | None = None,
        phone_number: str | None = None,
        tag: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsResponse:
        """Outbound WhatsApp lifecycle counts, one row per hour, gap-filled. Every event a message produces counts against the hour that message was accepted, so a row is a funnel over that hour's sends and recent rows under-report while callbacks arrive. Each row carries its own latency percentiles, but delivery, failure and read rates are whole-window figures: read those from `whatsapp.stats.summary`. Use it where the daily grain hides the shape; a window spans at most 720 hours (30 days), and for anything longer use `whatsapp.stats.daily`. Accepts the same single-dimension restriction: `template`, `category`, `phone_number` or `tag`. `from` and `to` are optional RFC 3339 instants, defaulting to the trailing 168 hours.

        ```python
        stats = await client.whatsapp.stats.hourly(from_="2026-08-30T00:00:00Z", to="2026-08-31T00:00:00Z")
        for point in stats.data or []:
            print(point.bucket, point.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/hourly",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "template": template,
                "category": category,
                "phone_number": phone_number,
                "tag": tag,
            },
            WhatsAppStatsResponse,
            options,
        )

    async def by_error_code(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByErrorCodeResponse:
        """Failed outbound WhatsApp messages grouped by the normalized failure reason, the same value a failed message reports in `last_error.code`, ranked by volume. This is the read that answers "why are sends failing"; `whatsapp.stats.summary` gives the failure rate, this gives its composition. A send declined before handoff (insufficient balance, missing price, suppressed recipient) counts as rejected rather than failed and is absent here; read `delivery.rejected` on `whatsapp.stats.summary` for it. Rows are the whole workspace's: the breakdown takes no dimension filter. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = await client.whatsapp.stats.by_error_code(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.error_code, row.count)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/error-codes",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByErrorCodeResponse,
            options,
        )

    async def by_template(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByTemplateResponse:
        """Outbound WhatsApp counts grouped by template, keyed by `template_id`, ranked by accepted volume. Resolve the id through the WhatsApp template endpoints for a name. Rows sum to less than the period's accepted total: a free-form send resolves no template, and some volume cannot be attributed to one at all, so take the denominator from `whatsapp.stats.summary`. Rows are the whole workspace's: the breakdown takes no dimension filter; to see one template over time, filter `whatsapp.stats.daily` by `template` instead. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = await client.whatsapp.stats.by_template(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.template_id, row.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/templates",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByTemplateResponse,
            options,
        )

    async def by_template_category(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByTemplateCategoryResponse:
        """Outbound WhatsApp counts grouped by Meta's template category (`authentication`, `utility` and `marketing` today, and the vocabulary is open), ranked by accepted volume. Category is what WhatsApp prices a message on, so this is the breakdown that explains a spend change. A free-form send carries no category, so rows sum to less than the period's accepted total; take the denominator from `whatsapp.stats.summary`. Rows are the whole workspace's: the breakdown takes no dimension filter. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = await client.whatsapp.stats.by_template_category(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.category, row.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/template-categories",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByTemplateCategoryResponse,
            options,
        )

    async def by_tag(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByTagResponse:
        """Outbound WhatsApp counts grouped by the tags a send attached, ranked by accepted volume. A row's tag is `name:value`, so one tag name spreads over as many rows as it has values. Rows are the whole workspace's: the breakdown takes no dimension filter; to see one tag over time, filter `whatsapp.stats.daily` by `tag` instead. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = await client.whatsapp.stats.by_tag(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.tag, row.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/tags",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByTagResponse,
            options,
        )

    async def by_phone_number(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByPhoneNumberResponse:
        """Outbound WhatsApp counts grouped by the business number that sent them, ranked by accepted volume, each row marked as a number your workspace connected or one Bird manages on your behalf. Use it to find the sender behind a delivery problem: a row identifies the number in E.164 form. Rows are the whole workspace's: the breakdown takes no dimension filter.

        ```python
        stats = await client.whatsapp.stats.by_phone_number(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.phone_number, row.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/phone-numbers",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByPhoneNumberResponse,
            options,
        )

    async def by_country(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppStatsByCountryResponse:
        """Outbound WhatsApp delivery, read and latency stats grouped by the recipient's country, ranked by accepted volume. Use it to compare markets. A weak market cannot be narrowed further: no breakdown takes a country filter, so `whatsapp.stats.by_error_code` reports the workspace's failure reasons rather than the selected country's, and pairing the two is inference, not attribution. A recipient whose country cannot be resolved is counted under `ZZ`, so one-to-one sends always land somewhere. Group sends are the exception and are absent by design: one spans up to eight countries and has no single destination, so rows sum to `whatsapp.stats.summary` less that group volume. Do not report the difference as missing data. Rows are the whole workspace's: the breakdown takes no dimension filter.

        ```python
        stats = await client.whatsapp.stats.by_country(from_="2026-08-01", to="2026-08-31")
        for row in stats.data or []:
            print(row.country, row.delivery)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/countries",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppStatsByCountryResponse,
            options,
        )
