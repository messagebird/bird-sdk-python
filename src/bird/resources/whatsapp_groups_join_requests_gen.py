# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from collections.abc import Sequence
from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppGroupJoinRequest,
    WhatsAppGroupJoinRequestDecision,
    WhatsAppGroupJoinRequestDecisionResult,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class WhatsappGroupsJoinRequestsListParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.groups.join_requests.list``. Every key is optional."""

    sort: str
    order: str
    limit: int
    starting_after: str
    ending_before: str


class _WhatsappGroupsJoinRequestsApproveRequired(TypedDict):
    join_request_ids: Sequence[str]


class WhatsappGroupsJoinRequestsApproveParams(_WhatsappGroupsJoinRequestsApproveRequired, total=False):
    """Params for ``client.whatsapp.groups.join_requests.approve``. ``join_request_ids`` is required."""


class _WhatsappGroupsJoinRequestsRejectRequired(TypedDict):
    join_request_ids: Sequence[str]


class WhatsappGroupsJoinRequestsRejectParams(_WhatsappGroupsJoinRequestsRejectRequired, total=False):
    """Params for ``client.whatsapp.groups.join_requests.reject``. ``join_request_ids`` is required."""


class WhatsappGroupsJoinRequests(Resource):
    def list(
        self,
        group_id: str,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[WhatsAppGroupJoinRequest]:
        """List the people waiting to be let into a group, oldest first, as a cursor page. Only a group created with join_approval_mode approval_required collects them. Decide them with the batch-approve and batch-reject operations.

        ```python
        for request in client.whatsapp.groups.join_requests.list("wag_01krdgeqcxet5s7t44vh8rt9mg"):
            print(request.id, request.bsuid)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, f"/v1/whatsapp/groups/{quote(group_id, safe='')}/join-requests", query, WhatsAppGroupJoinRequest, options)

    def approve(
        self,
        group_id: str,
        *,
        join_request_ids: Sequence[str],
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupJoinRequestDecisionResult:
        """Approve up to 50 pending join requests in one call. WhatsApp decides each one on its own, so the response splits them into decided and failed; a failure on one leaves the rest applied.

        ```python
        result = client.whatsapp.groups.join_requests.approve(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            join_request_ids=["wgj_01krdgeqcxet5s7t44vh8rt9mg"],
        )
        print(len(result.decided), len(result.failed))
        ```
        """
        body = to_wire(
            WhatsAppGroupJoinRequestDecision,
            {
                "join_request_ids": join_request_ids,
            },
        )
        return self._write(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/join-requests/batch-approve",
            body,
            WhatsAppGroupJoinRequestDecisionResult,
            options,
        )

    def reject(
        self,
        group_id: str,
        *,
        join_request_ids: Sequence[str],
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupJoinRequestDecisionResult:
        """Reject up to 50 pending join requests in one call. WhatsApp decides each one on its own, so the response splits them into decided and failed; a failure on one leaves the rest applied. A group that is not active refuses the whole call with a 409 WhatsAppGroupNotActive.

        ```python
        result = client.whatsapp.groups.join_requests.reject(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            join_request_ids=["wgj_01krdgeqcxet5s7t44vh8rt9mg"],
        )
        for failure in result.failed:
            print(failure.join_request_id, failure.error.description)
        ```
        """
        body = to_wire(
            WhatsAppGroupJoinRequestDecision,
            {
                "join_request_ids": join_request_ids,
            },
        )
        return self._write(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/join-requests/batch-reject",
            body,
            WhatsAppGroupJoinRequestDecisionResult,
            options,
        )


class AsyncWhatsappGroupsJoinRequests(AsyncResource):
    def list(
        self,
        group_id: str,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[WhatsAppGroupJoinRequest]:
        """List the people waiting to be let into a group, oldest first, as a cursor page. Only a group created with join_approval_mode approval_required collects them. Decide them with the batch-approve and batch-reject operations.

        ```python
        async for request in client.whatsapp.groups.join_requests.list("wag_01krdgeqcxet5s7t44vh8rt9mg"):
            print(request.id, request.bsuid)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, f"/v1/whatsapp/groups/{quote(group_id, safe='')}/join-requests", query, WhatsAppGroupJoinRequest, options)

    async def approve(
        self,
        group_id: str,
        *,
        join_request_ids: Sequence[str],
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupJoinRequestDecisionResult:
        """Approve up to 50 pending join requests in one call. WhatsApp decides each one on its own, so the response splits them into decided and failed; a failure on one leaves the rest applied.

        ```python
        result = await client.whatsapp.groups.join_requests.approve(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            join_request_ids=["wgj_01krdgeqcxet5s7t44vh8rt9mg"],
        )
        print(len(result.decided), len(result.failed))
        ```
        """
        body = to_wire(
            WhatsAppGroupJoinRequestDecision,
            {
                "join_request_ids": join_request_ids,
            },
        )
        return await self._write(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/join-requests/batch-approve",
            body,
            WhatsAppGroupJoinRequestDecisionResult,
            options,
        )

    async def reject(
        self,
        group_id: str,
        *,
        join_request_ids: Sequence[str],
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupJoinRequestDecisionResult:
        """Reject up to 50 pending join requests in one call. WhatsApp decides each one on its own, so the response splits them into decided and failed; a failure on one leaves the rest applied. A group that is not active refuses the whole call with a 409 WhatsAppGroupNotActive.

        ```python
        result = await client.whatsapp.groups.join_requests.reject(
            "wag_01krdgeqcxet5s7t44vh8rt9mg",
            join_request_ids=["wgj_01krdgeqcxet5s7t44vh8rt9mg"],
        )
        for failure in result.failed:
            print(failure.join_request_id, failure.error.description)
        ```
        """
        body = to_wire(
            WhatsAppGroupJoinRequestDecision,
            {
                "join_request_ids": join_request_ids,
            },
        )
        return await self._write(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/join-requests/batch-reject",
            body,
            WhatsAppGroupJoinRequestDecisionResult,
            options,
        )
