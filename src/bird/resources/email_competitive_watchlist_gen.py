# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict

from bird._generated import (
    EmailCompetitiveNotableFeed,
    EmailCompetitiveWatchlist,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class EmailCompetitiveWatchlistGetParams(TypedDict, total=False):
    """Query params for ``client.email.competitive.watchlist.get``. Every key is optional."""

    range: int


class EmailCompetitiveWatchlistNotableParams(TypedDict, total=False):
    """Query params for ``client.email.competitive.watchlist.notable``. Every key is optional."""

    range: int


class EmailCompetitiveWatchlistBase(Resource):
    def get(
        self,
        *,
        range: int | None = None,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveWatchlist:
        """Read the complete watchlist report for the requested range. Competitor rows carry `watchlist_brand_id` values for `email.competitive.watchlist.brands.get` and sampled panel figures; an unavailable figure is null rather than zero. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        report = client.email.competitive.watchlist.get(range=30)
        print(report.model_dump_json())
        ```
        """
        return self._get(
            "/v1/email/competitive/watchlist",
            {
                "range": range,
            },
            EmailCompetitiveWatchlist,
            options,
        )

    def notable(
        self,
        *,
        range: int | None = None,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveNotableFeed:
        """Returns up to 100 findings across watched brands for the requested range. A campaign can appear for multiple signals, each with its own evidence. truncated reports omitted eligible findings; this report has no cursor pages. Each finding carries the watched-brand and campaign IDs for `email.competitive.watchlist.brands.campaigns.get`. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        report = client.email.competitive.watchlist.notable(range=30)
        print(report.model_dump_json())
        ```
        """
        return self._get(
            "/v1/email/competitive/watchlist/notable",
            {
                "range": range,
            },
            EmailCompetitiveNotableFeed,
            options,
        )


class AsyncEmailCompetitiveWatchlistBase(AsyncResource):
    async def get(
        self,
        *,
        range: int | None = None,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveWatchlist:
        """Read the complete watchlist report for the requested range. Competitor rows carry `watchlist_brand_id` values for `email.competitive.watchlist.brands.get` and sampled panel figures; an unavailable figure is null rather than zero. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        report = await client.email.competitive.watchlist.get(range=30)
        print(report.model_dump_json())
        ```
        """
        return await self._get(
            "/v1/email/competitive/watchlist",
            {
                "range": range,
            },
            EmailCompetitiveWatchlist,
            options,
        )

    async def notable(
        self,
        *,
        range: int | None = None,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveNotableFeed:
        """Returns up to 100 findings across watched brands for the requested range. A campaign can appear for multiple signals, each with its own evidence. truncated reports omitted eligible findings; this report has no cursor pages. Each finding carries the watched-brand and campaign IDs for `email.competitive.watchlist.brands.campaigns.get`. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        report = await client.email.competitive.watchlist.notable(range=30)
        print(report.model_dump_json())
        ```
        """
        return await self._get(
            "/v1/email/competitive/watchlist/notable",
            {
                "range": range,
            },
            EmailCompetitiveNotableFeed,
            options,
        )
