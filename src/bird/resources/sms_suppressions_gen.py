# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    SMSSuppression,
    SMSSuppressionCreate,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class SmsSuppressionsListParams(TypedDict, total=False):
    """Query params for ``client.sms_suppressions.list``. Every key is optional."""

    destination: str
    originator: str
    reason: str
    limit: int
    starting_after: str
    ending_before: str


class _SmsSuppressionsAddRequired(TypedDict):
    destination: str
    originator: str


class SmsSuppressionsAddParams(_SmsSuppressionsAddRequired, total=False):
    """Params for ``client.sms_suppressions.add``. ``destination`` and ``originator`` are required."""


class SmsSuppressions(Resource):
    def list(
        self,
        *,
        destination: str | None = None,
        originator: str | None = None,
        reason: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[SMSSuppression]:
        """List the workspace's SMS suppressions (sender-and-subscriber pairs blocked from delivery) as a cursor page."""
        query = {
            "destination": destination,
            "originator": originator,
            "reason": reason,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/sms/suppressions", query, SMSSuppression, options)

    def get(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSSuppression:
        """Read one SMS suppression: the sender and subscriber it covers, why messages are stopped, what it blocks, and whether it is still in force. To check whether you may message someone, filter `sms_suppressions.list` by their number instead."""
        return self._get(
            f"/v1/sms/suppressions/{quote(suppression_id, safe='')}",
            {},
            SMSSuppression,
            options,
        )

    def add(
        self,
        *,
        destination: str,
        originator: str,
        options: RequestOptions | None = None,
    ) -> SMSSuppression:
        """Stop one of your senders from messaging one subscriber. Covers that sender only; your other senders keep reaching them."""
        body = to_wire(
            SMSSuppressionCreate,
            {
                "destination": destination,
                "originator": originator,
            },
        )
        return self._write(
            "POST",
            "/v1/sms/suppressions",
            body,
            SMSSuppression,
            options,
        )

    def remove(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """End a manual SMS suppression, letting that sender message that subscriber again. Only reason `manual` can be ended this way: a subscriber's own stop keyword and a carrier's opt-out are refused."""
        self._delete(f"/v1/sms/suppressions/{quote(suppression_id, safe='')}", options)


class AsyncSmsSuppressions(AsyncResource):
    def list(
        self,
        *,
        destination: str | None = None,
        originator: str | None = None,
        reason: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[SMSSuppression]:
        """List the workspace's SMS suppressions (sender-and-subscriber pairs blocked from delivery) as a cursor page."""
        query = {
            "destination": destination,
            "originator": originator,
            "reason": reason,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/sms/suppressions", query, SMSSuppression, options)

    async def get(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSSuppression:
        """Read one SMS suppression: the sender and subscriber it covers, why messages are stopped, what it blocks, and whether it is still in force. To check whether you may message someone, filter `sms_suppressions.list` by their number instead."""
        return await self._get(
            f"/v1/sms/suppressions/{quote(suppression_id, safe='')}",
            {},
            SMSSuppression,
            options,
        )

    async def add(
        self,
        *,
        destination: str,
        originator: str,
        options: RequestOptions | None = None,
    ) -> SMSSuppression:
        """Stop one of your senders from messaging one subscriber. Covers that sender only; your other senders keep reaching them."""
        body = to_wire(
            SMSSuppressionCreate,
            {
                "destination": destination,
                "originator": originator,
            },
        )
        return await self._write(
            "POST",
            "/v1/sms/suppressions",
            body,
            SMSSuppression,
            options,
        )

    async def remove(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """End a manual SMS suppression, letting that sender message that subscriber again. Only reason `manual` can be ended this way: a subscriber's own stop keyword and a carrier's opt-out are refused."""
        await self._delete(f"/v1/sms/suppressions/{quote(suppression_id, safe='')}", options)
