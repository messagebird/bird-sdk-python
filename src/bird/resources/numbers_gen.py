# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from collections.abc import Sequence
from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    Number,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class NumbersListParams(TypedDict, total=False):
    """Query params for ``client.numbers.list``. Every key is optional."""

    number: str
    country_code: str
    number_type: str
    prefix: str
    capabilities: Sequence[str]
    limit: int
    starting_after: str
    ending_before: str


class NumbersBase(Resource):
    def list(
        self,
        *,
        number: str | None = None,
        country_code: str | None = None,
        number_type: str | None = None,
        prefix: str | None = None,
        capabilities: Sequence[str] | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[Number]:
        """Pages the numbers allocated to the workspace, dedicated and shared alike. Narrows on country, type, prefix and capability, so one number is reached without walking every page.

        ```python
        for allocated in client.numbers.list(country_code="GB"):
            # kind tells a number you bought from one Bird manages for several
            # workspaces.
            print(allocated.number, allocated.kind, allocated.status)
        ```
        """
        query = {
            "number": number,
            "country_code": country_code,
            "number_type": number_type,
            "prefix": prefix,
            "capabilities": capabilities,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/numbers", query, Number, options)

    def get(
        self,
        number_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> Number:
        """Reads one allocated number by the id `numbers.list` returns. Carries its status and, where a country demands ownership paperwork, what is still outstanding on it.

        ```python
        allocated = client.numbers.get("nda_01krdgeqcxet5s7t44vh8rt9mg")
        # A country that asks for ownership paperwork answers here; most answer None.
        print(allocated.status, allocated.ownership or "no paperwork required")
        ```
        """
        return self._get(
            f"/v1/numbers/{quote(number_id, safe='')}",
            {},
            Number,
            options,
        )

    def release(
        self,
        number_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Gives a dedicated number back and stops its monthly charge. Irreversible: the number leaves the workspace and the channels built on it stop sending. A shared number cannot be released.

        ```python
        # Releasing stops the monthly charge and the number stops working for you.
        # Only a dedicated number can be released; a shared one answers E14002.
        client.numbers.release("nda_01krdgeqcxet5s7t44vh8rt9mg")
        ```
        """
        self._delete(f"/v1/numbers/{quote(number_id, safe='')}", options)


class AsyncNumbersBase(AsyncResource):
    def list(
        self,
        *,
        number: str | None = None,
        country_code: str | None = None,
        number_type: str | None = None,
        prefix: str | None = None,
        capabilities: Sequence[str] | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[Number]:
        """Pages the numbers allocated to the workspace, dedicated and shared alike. Narrows on country, type, prefix and capability, so one number is reached without walking every page.

        ```python
        async for allocated in client.numbers.list(country_code="GB"):
            # kind tells a number you bought from one Bird manages for several
            # workspaces.
            print(allocated.number, allocated.kind, allocated.status)
        ```
        """
        query = {
            "number": number,
            "country_code": country_code,
            "number_type": number_type,
            "prefix": prefix,
            "capabilities": capabilities,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/numbers", query, Number, options)

    async def get(
        self,
        number_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> Number:
        """Reads one allocated number by the id `numbers.list` returns. Carries its status and, where a country demands ownership paperwork, what is still outstanding on it.

        ```python
        allocated = await client.numbers.get("nda_01krdgeqcxet5s7t44vh8rt9mg")
        # A country that asks for ownership paperwork answers here; most answer None.
        print(allocated.status, allocated.ownership or "no paperwork required")
        ```
        """
        return await self._get(
            f"/v1/numbers/{quote(number_id, safe='')}",
            {},
            Number,
            options,
        )

    async def release(
        self,
        number_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Gives a dedicated number back and stops its monthly charge. Irreversible: the number leaves the workspace and the channels built on it stop sending. A shared number cannot be released.

        ```python
        # Releasing stops the monthly charge and the number stops working for you.
        # Only a dedicated number can be released; a shared one answers E14002.
        await client.numbers.release("nda_01krdgeqcxet5s7t44vh8rt9mg")
        ```
        """
        await self._delete(f"/v1/numbers/{quote(number_id, safe='')}", options)
