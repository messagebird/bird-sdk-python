# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from collections.abc import Sequence
from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppTemplate,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class WhatsappTemplatesListParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.templates.list``. Every key is optional."""

    waba: str
    status: Sequence[str]
    scope: str
    category: str
    q: str
    limit: int
    starting_after: str
    ending_before: str


class WhatsappTemplatesBase(Resource):
    def list(
        self,
        *,
        waba: str | None = None,
        status: Sequence[str] | None = None,
        scope: str | None = None,
        category: str | None = None,
        q: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[WhatsAppTemplate]:
        """List the workspace's WhatsApp templates: each template's handle, its lifecycle, and where each of its languages stands. Content is not included; use `whatsapp.templates.versions.get` for that.

        ```python
        for tpl in client.whatsapp.templates.list():
            print(tpl.slug, tpl.status)
        ```
        """
        query = {
            "waba": waba,
            "status": status,
            "scope": scope,
            "category": category,
            "q": q,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/whatsapp/templates", query, WhatsAppTemplate, options)

    def get(
        self,
        template_ref: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplate:
        """Get one WhatsApp template by slug or ID: its handle, its lifecycle, and where each of its languages stands. Content is not included; use `whatsapp.templates.versions.get` for that.

        ```python
        tpl = client.whatsapp.templates.get("bird_otp")
        print(tpl.default_language, tpl.available_languages)
        ```
        """
        return self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}",
            {},
            WhatsAppTemplate,
            options,
        )


class AsyncWhatsappTemplatesBase(AsyncResource):
    def list(
        self,
        *,
        waba: str | None = None,
        status: Sequence[str] | None = None,
        scope: str | None = None,
        category: str | None = None,
        q: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[WhatsAppTemplate]:
        """List the workspace's WhatsApp templates: each template's handle, its lifecycle, and where each of its languages stands. Content is not included; use `whatsapp.templates.versions.get` for that.

        ```python
        async for tpl in client.whatsapp.templates.list():
            print(tpl.slug, tpl.status)
        ```
        """
        query = {
            "waba": waba,
            "status": status,
            "scope": scope,
            "category": category,
            "q": q,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/whatsapp/templates", query, WhatsAppTemplate, options)

    async def get(
        self,
        template_ref: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppTemplate:
        """Get one WhatsApp template by slug or ID: its handle, its lifecycle, and where each of its languages stands. Content is not included; use `whatsapp.templates.versions.get` for that.

        ```python
        tpl = await client.whatsapp.templates.get("bird_otp")
        print(tpl.default_language, tpl.available_languages)
        ```
        """
        return await self._get(
            f"/v1/whatsapp/templates/{quote(template_ref, safe='')}",
            {},
            WhatsAppTemplate,
            options,
        )
