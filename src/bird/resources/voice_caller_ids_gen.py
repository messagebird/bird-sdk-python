# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    VoiceCallerID,
    VoiceCallerIDVerifyRequest,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class VoiceCallerIdsListParams(TypedDict, total=False):
    """Query params for ``client.voice.caller_ids.list``. Every key is optional."""

    sort: str
    order: str
    limit: int
    starting_after: str
    ending_before: str


class _VoiceCallerIdsVerifyRequired(TypedDict):
    code: str


class VoiceCallerIdsVerifyParams(_VoiceCallerIdsVerifyRequired, total=False):
    """Params for ``client.voice.caller_ids.verify``. ``code`` is required."""


class VoiceCallerIds(Resource):
    def list(
        self,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[VoiceCallerID]:
        """List the workspace's caller IDs and their verification status, as a cursor page. Only a verified caller ID may be presented on an outbound call, so an unverified or missing entry here is a common reason a call is refused.

        ```python
        for caller_id in client.voice.caller_ids.list():
            print(caller_id.id, caller_id.phone_number, caller_id.status)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/voice/caller-ids", query, VoiceCallerID, options)

    def get(
        self,
        caller_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceCallerID:
        """Fetch one caller ID by ID, with its verification status. Use `voice.caller_ids.list` to find the ID.

        ```python
        caller_id = client.voice.caller_ids.get("caller-id")
        print(caller_id.phone_number, caller_id.status, caller_id.verified_at)
        ```
        """
        return self._get(
            f"/v1/voice/caller-ids/{quote(caller_id, safe='')}",
            {},
            VoiceCallerID,
            options,
        )

    def verify(
        self,
        caller_id: str,
        *,
        code: str,
        options: RequestOptions | None = None,
    ) -> VoiceCallerID:
        """Complete a caller-ID verification challenge started in the dashboard. Recovery can place another verification call to the same number and requires the organization to remain eligible to register caller IDs. For expired or exhausted challenges, ask the user to select Get a new code in the dashboard. List caller IDs again to obtain the replacement registration ID before submitting its code. Read Voice destinations separately to confirm whether the number's country is enabled.

        ```python
        caller_id = client.voice.caller_ids.verify("CALLER_ID", code="123456")
        print(caller_id.id, caller_id.status)
        ```
        """
        body = to_wire(
            VoiceCallerIDVerifyRequest,
            {
                "code": code,
            },
        )
        return self._write(
            "POST",
            f"/v1/voice/caller-ids/{quote(caller_id, safe='')}/verify",
            body,
            VoiceCallerID,
            options,
        )


class AsyncVoiceCallerIds(AsyncResource):
    def list(
        self,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[VoiceCallerID]:
        """List the workspace's caller IDs and their verification status, as a cursor page. Only a verified caller ID may be presented on an outbound call, so an unverified or missing entry here is a common reason a call is refused.

        ```python
        async for caller_id in client.voice.caller_ids.list():
            print(caller_id.id, caller_id.phone_number, caller_id.status)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/voice/caller-ids", query, VoiceCallerID, options)

    async def get(
        self,
        caller_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceCallerID:
        """Fetch one caller ID by ID, with its verification status. Use `voice.caller_ids.list` to find the ID.

        ```python
        caller_id = await client.voice.caller_ids.get("caller-id")
        print(caller_id.phone_number, caller_id.status, caller_id.verified_at)
        ```
        """
        return await self._get(
            f"/v1/voice/caller-ids/{quote(caller_id, safe='')}",
            {},
            VoiceCallerID,
            options,
        )

    async def verify(
        self,
        caller_id: str,
        *,
        code: str,
        options: RequestOptions | None = None,
    ) -> VoiceCallerID:
        """Complete a caller-ID verification challenge started in the dashboard. Recovery can place another verification call to the same number and requires the organization to remain eligible to register caller IDs. For expired or exhausted challenges, ask the user to select Get a new code in the dashboard. List caller IDs again to obtain the replacement registration ID before submitting its code. Read Voice destinations separately to confirm whether the number's country is enabled.

        ```python
        caller_id = await client.voice.caller_ids.verify("CALLER_ID", code="123456")
        print(caller_id.id, caller_id.status)
        ```
        """
        body = to_wire(
            VoiceCallerIDVerifyRequest,
            {
                "code": code,
            },
        )
        return await self._write(
            "POST",
            f"/v1/voice/caller-ids/{quote(caller_id, safe='')}/verify",
            body,
            VoiceCallerID,
            options,
        )
