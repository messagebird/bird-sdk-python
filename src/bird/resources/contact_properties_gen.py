# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from bird._generated import (
    ContactProperty,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class ContactPropertiesBase(Resource):
    def list(
        self,
        *,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[ContactProperty]:
        """List the workspace's contact properties as a cursor page, newest first. Archived properties are included, marked by their archived flag.

        ```python
        for prop in client.contact_properties.list():
            print(prop.id, prop.key)
        ```
        """
        query = {
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/contact-properties", query, ContactProperty, options)

    def get(
        self,
        property_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> ContactProperty:
        """Get a single contact property by ID: key, type, fallback value, and archived state.

        ```python
        prop = client.contact_properties.get("prp_01krdgeqcxet5s7t44vh8rt9mg")
        print(prop.key, prop.type)
        ```
        """
        return self._get(
            f"/v1/contact-properties/{property_id}",
            {},
            ContactProperty,
            options,
        )

    def archive(
        self,
        property_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> ContactProperty:
        """Archive a contact property: the key is rejected in new contact writes and stops rendering in templates, while stored values remain readable. The key stays reserved and counts toward the 200-property limit; reverse with `contact_properties.unarchive`.

        ```python
        prop = client.contact_properties.archive("prp_01krdgeqcxet5s7t44vh8rt9mg")
        print(prop.key, prop.archived)
        ```
        """
        return self._action(
            "POST",
            f"/v1/contact-properties/{property_id}/archive",
            ContactProperty,
            options,
        )

    def unarchive(
        self,
        property_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> ContactProperty:
        """Reactivate an archived contact property so its key is accepted in contact writes and renders in templates again. Fails with a conflict if the property is not archived.

        ```python
        prop = client.contact_properties.unarchive("prp_01krdgeqcxet5s7t44vh8rt9mg")
        print(prop.archived)
        ```
        """
        return self._action(
            "POST",
            f"/v1/contact-properties/{property_id}/unarchive",
            ContactProperty,
            options,
        )


class AsyncContactPropertiesBase(AsyncResource):
    def list(
        self,
        *,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[ContactProperty]:
        """List the workspace's contact properties as a cursor page, newest first. Archived properties are included, marked by their archived flag.

        ```python
        async for prop in client.contact_properties.list():
            print(prop.id, prop.key)
        ```
        """
        query = {
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/contact-properties", query, ContactProperty, options)

    async def get(
        self,
        property_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> ContactProperty:
        """Get a single contact property by ID: key, type, fallback value, and archived state.

        ```python
        prop = await client.contact_properties.get("prp_01krdgeqcxet5s7t44vh8rt9mg")
        print(prop.key, prop.type)
        ```
        """
        return await self._get(
            f"/v1/contact-properties/{property_id}",
            {},
            ContactProperty,
            options,
        )

    async def archive(
        self,
        property_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> ContactProperty:
        """Archive a contact property: the key is rejected in new contact writes and stops rendering in templates, while stored values remain readable. The key stays reserved and counts toward the 200-property limit; reverse with `contact_properties.unarchive`.

        ```python
        prop = await client.contact_properties.archive("prp_01krdgeqcxet5s7t44vh8rt9mg")
        print(prop.key, prop.archived)
        ```
        """
        return await self._action(
            "POST",
            f"/v1/contact-properties/{property_id}/archive",
            ContactProperty,
            options,
        )

    async def unarchive(
        self,
        property_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> ContactProperty:
        """Reactivate an archived contact property so its key is accepted in contact writes and renders in templates again. Fails with a conflict if the property is not archived.

        ```python
        prop = await client.contact_properties.unarchive("prp_01krdgeqcxet5s7t44vh8rt9mg")
        print(prop.archived)
        ```
        """
        return await self._action(
            "POST",
            f"/v1/contact-properties/{property_id}/unarchive",
            ContactProperty,
            options,
        )
