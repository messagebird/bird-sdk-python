# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    Preference,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class PreferencesListParams(TypedDict, total=False):
    """Query params for ``client.preferences.list``. Every key is optional."""

    channel: str
    handle: str
    limit: int
    starting_after: str
    ending_before: str


class PreferencesBase(Resource):
    def list(
        self,
        *,
        channel: str | None = None,
        handle: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[Preference]:
        """List the workspace's stated messaging preferences (consent grants and opt-outs) as a cursor page. Filter by channel, and by handle within a channel, to look up one person before messaging them.

        ```python
        for preference in client.preferences.list(channel="email"):
            print(preference.id, preference.handle, preference.status)
        ```
        """
        query = {
            "channel": channel,
            "handle": handle,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/preferences", query, Preference, options)

    def get(
        self,
        preference_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> Preference:
        """Read one recorded preference by ID: the channel and handle it is about, whether it grants or revokes, how much traffic it covers, and where the statement came from.

        ```python
        preference = client.preferences.get("prf_01krdgeqcxet5s7t44vh8rt9mg")
        print(preference.status, preference.coverage)
        ```
        """
        return self._get(
            f"/v1/preferences/{quote(preference_id, safe='')}",
            {},
            Preference,
            options,
        )


class AsyncPreferencesBase(AsyncResource):
    def list(
        self,
        *,
        channel: str | None = None,
        handle: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[Preference]:
        """List the workspace's stated messaging preferences (consent grants and opt-outs) as a cursor page. Filter by channel, and by handle within a channel, to look up one person before messaging them.

        ```python
        async for preference in client.preferences.list(channel="email"):
            print(preference.id, preference.handle, preference.status)
        ```
        """
        query = {
            "channel": channel,
            "handle": handle,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/preferences", query, Preference, options)

    async def get(
        self,
        preference_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> Preference:
        """Read one recorded preference by ID: the channel and handle it is about, whether it grants or revokes, how much traffic it covers, and where the statement came from.

        ```python
        preference = await client.preferences.get("prf_01krdgeqcxet5s7t44vh8rt9mg")
        print(preference.status, preference.coverage)
        ```
        """
        return await self._get(
            f"/v1/preferences/{quote(preference_id, safe='')}",
            {},
            Preference,
            options,
        )
