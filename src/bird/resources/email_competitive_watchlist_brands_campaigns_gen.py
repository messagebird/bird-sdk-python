# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    EmailCompetitiveCampaign,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class EmailCompetitiveWatchlistBrandsCampaignsListParams(TypedDict, total=False):
    """Query params for ``client.email.competitive.watchlist.brands.campaigns.list``. Every key is optional."""

    range: int
    sort: str
    order: str
    limit: int
    starting_after: str
    ending_before: str


class EmailCompetitiveWatchlistBrandsCampaigns(Resource):
    def list(
        self,
        watchlist_brand_id: str,
        *,
        range: int | None = None,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[EmailCompetitiveCampaign]:
        """Takes a watchlist_brand_id from `email.competitive.watchlist.get`. Returns a cursor page, default 25 rows and maximum 100. Cursors retain the brand, range and ordering: next_cursor advances with starting_after; prev_cursor or refresh_cursor reads preceding rows with ending_before. captured and promo_rate describe a sample independently of this page. next_cursor controls continuation; truncated can also describe the sample. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        watchlist = client.email.competitive.watchlist.get(range=30)
        entry = next((row for row in watchlist.data if row.name == "Everlane" and row.watchlist_brand_id), None)
        if entry is None or entry.watchlist_brand_id is None:
            raise ValueError("Add Everlane to the watchlist first")
        watchlist_brand_id = entry.watchlist_brand_id
        for campaign in client.email.competitive.watchlist.brands.campaigns.list(watchlist_brand_id, range=30, limit=25):
            print(campaign.id)
        ```
        """
        query = {
            "range": range,
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, f"/v1/email/competitive/watchlist/brands/{quote(watchlist_brand_id, safe='')}/campaigns", query, EmailCompetitiveCampaign, options)

    def get(
        self,
        watchlist_brand_id: str,
        campaign_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveCampaign:
        """Takes a watchlist_brand_id and a campaign_id from `email.competitive.watchlist.brands.campaigns.list`. campaign_id is text to preserve its digits. Returns the campaign's figures and nullable creative URL; a campaign outside that watched brand returns not found. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        watchlist = client.email.competitive.watchlist.get(range=30)
        entry = next((row for row in watchlist.data if row.name == "Everlane" and row.watchlist_brand_id), None)
        if entry is None or entry.watchlist_brand_id is None:
            raise ValueError("Add Everlane to the watchlist first")
        watchlist_brand_id = entry.watchlist_brand_id
        campaign_id = None
        for campaign in client.email.competitive.watchlist.brands.campaigns.list(watchlist_brand_id, range=30, limit=1):
            campaign_id = campaign.id
            break
        if campaign_id is None:
            raise ValueError("No captured campaigns")
        report = client.email.competitive.watchlist.brands.campaigns.get(watchlist_brand_id, campaign_id)
        print(report.model_dump_json())
        ```
        """
        return self._get(
            f"/v1/email/competitive/watchlist/brands/{quote(watchlist_brand_id, safe='')}/campaigns/{quote(campaign_id, safe='')}",
            {},
            EmailCompetitiveCampaign,
            options,
        )


class AsyncEmailCompetitiveWatchlistBrandsCampaigns(AsyncResource):
    def list(
        self,
        watchlist_brand_id: str,
        *,
        range: int | None = None,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[EmailCompetitiveCampaign]:
        """Takes a watchlist_brand_id from `email.competitive.watchlist.get`. Returns a cursor page, default 25 rows and maximum 100. Cursors retain the brand, range and ordering: next_cursor advances with starting_after; prev_cursor or refresh_cursor reads preceding rows with ending_before. captured and promo_rate describe a sample independently of this page. next_cursor controls continuation; truncated can also describe the sample. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        watchlist = await client.email.competitive.watchlist.get(range=30)
        entry = next((row for row in watchlist.data if row.name == "Everlane" and row.watchlist_brand_id), None)
        if entry is None or entry.watchlist_brand_id is None:
            raise ValueError("Add Everlane to the watchlist first")
        watchlist_brand_id = entry.watchlist_brand_id
        async for campaign in client.email.competitive.watchlist.brands.campaigns.list(watchlist_brand_id, range=30, limit=25):
            print(campaign.id)
        ```
        """
        query = {
            "range": range,
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, f"/v1/email/competitive/watchlist/brands/{quote(watchlist_brand_id, safe='')}/campaigns", query, EmailCompetitiveCampaign, options)

    async def get(
        self,
        watchlist_brand_id: str,
        campaign_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveCampaign:
        """Takes a watchlist_brand_id and a campaign_id from `email.competitive.watchlist.brands.campaigns.list`. campaign_id is text to preserve its digits. Returns the campaign's figures and nullable creative URL; a campaign outside that watched brand returns not found. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        watchlist = await client.email.competitive.watchlist.get(range=30)
        entry = next((row for row in watchlist.data if row.name == "Everlane" and row.watchlist_brand_id), None)
        if entry is None or entry.watchlist_brand_id is None:
            raise ValueError("Add Everlane to the watchlist first")
        watchlist_brand_id = entry.watchlist_brand_id
        campaign_id = None
        async for campaign in client.email.competitive.watchlist.brands.campaigns.list(watchlist_brand_id, range=30, limit=1):
            campaign_id = campaign.id
            break
        if campaign_id is None:
            raise ValueError("No captured campaigns")
        report = await client.email.competitive.watchlist.brands.campaigns.get(watchlist_brand_id, campaign_id)
        print(report.model_dump_json())
        ```
        """
        return await self._get(
            f"/v1/email/competitive/watchlist/brands/{quote(watchlist_brand_id, safe='')}/campaigns/{quote(campaign_id, safe='')}",
            {},
            EmailCompetitiveCampaign,
            options,
        )
