# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from bird._generated import (
    VoiceSessionCredential,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class VoiceSessionCredentials(Resource):
    def create(
        self,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceSessionCredential:
        """Mint a short-lived SIP digest credential a calling client authenticates with. The password is returned once and cannot be retrieved again.

        ```python
        credential = client.voice.session_credentials.create()
        # The password is returned once. Until expires_at it can place billed calls.
        print(credential.username, credential.realm, credential.expires_at)
        ```
        """
        return self._action(
            "POST",
            "/v1/voice/session-credentials",
            VoiceSessionCredential,
            options,
        )


class AsyncVoiceSessionCredentials(AsyncResource):
    async def create(
        self,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceSessionCredential:
        """Mint a short-lived SIP digest credential a calling client authenticates with. The password is returned once and cannot be retrieved again.

        ```python
        credential = await client.voice.session_credentials.create()
        # The password is returned once. Until expires_at it can place billed calls.
        print(credential.username, credential.realm, credential.expires_at)
        ```
        """
        return await self._action(
            "POST",
            "/v1/voice/session-credentials",
            VoiceSessionCredential,
            options,
        )
