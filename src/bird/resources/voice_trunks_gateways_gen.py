# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    VoiceTrunkGateway,
    VoiceTrunkGatewayCreate,
    VoiceTrunkGatewayList,
    VoiceTrunkGatewayUpdate,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class _VoiceTrunksGatewaysCreateRequired(TypedDict):
    sip_uri: str
    priority: int


class VoiceTrunksGatewaysCreateParams(_VoiceTrunksGatewaysCreateRequired, total=False):
    """Params for ``client.voice.trunks.gateways.create``. ``sip_uri`` and ``priority`` are required."""

    origination_format: str
    destination_format: str


class VoiceTrunksGatewaysUpdateParams(TypedDict, total=False):
    """Params for ``client.voice.trunks.gateways.update``. Every key is optional."""

    sip_uri: str
    priority: int
    origination_format: str
    destination_format: str


class VoiceTrunksGateways(Resource):
    def list(
        self,
        trunk_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGatewayList:
        """Read the complete inbound gateway list for a trunk, ordered by priority. Available even when inbound calling is disabled.

        ```python
        gateways = client.voice.trunks.gateways.list("TRUNK_ID")
        for gateway in gateways.data:
            print(gateway.id, gateway.priority)
        ```
        """
        return self._get(
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways",
            {},
            VoiceTrunkGatewayList,
            options,
        )

    def get(
        self,
        trunk_id: str,
        gateway_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGateway:
        """Read one inbound gateway and its SIP address, priority, and number formats. The gateway must belong to the specified trunk in this workspace.

        ```python
        gateway = client.voice.trunks.gateways.get("TRUNK_ID", "GATEWAY_ID")
        print(gateway.id, gateway.priority)
        ```
        """
        return self._get(
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways/{quote(gateway_id, safe='')}",
            {},
            VoiceTrunkGateway,
            options,
        )

    def create(
        self,
        trunk_id: str,
        *,
        sip_uri: str,
        priority: int,
        origination_format: str | None = None,
        destination_format: str | None = None,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGateway:
        """Add an address for inbound call delivery. Enable inbound calling on the trunk first; a trunk supports up to 20 gateways.

        ```python
        gateway = client.voice.trunks.gateways.create(
            "TRUNK_ID",
            sip_uri="sip:pbx.example.com:5060",
            priority=0,
            destination_format="1234#{number}",
        )
        print(gateway.id, gateway.priority)
        ```
        """
        body = to_wire(
            VoiceTrunkGatewayCreate,
            {
                "sip_uri": sip_uri,
                "priority": priority,
                "origination_format": origination_format,
                "destination_format": destination_format,
            },
        )
        return self._write(
            "POST",
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways",
            body,
            VoiceTrunkGateway,
            options,
        )

    def update(
        self,
        trunk_id: str,
        gateway_id: str,
        *,
        sip_uri: str | None = None,
        priority: int | None = None,
        origination_format: str | None = None,
        destination_format: str | None = None,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGateway:
        """Change an inbound gateway address, priority, or number formats. Omitted fields stay unchanged; an empty format restores E.164. Requires inbound calling enabled.

        ```python
        gateway = client.voice.trunks.gateways.update("TRUNK_ID", "GATEWAY_ID", priority=10)
        print(gateway.id, gateway.priority)
        ```
        """
        body = to_wire(
            VoiceTrunkGatewayUpdate,
            {
                "sip_uri": sip_uri,
                "priority": priority,
                "origination_format": origination_format,
                "destination_format": destination_format,
            },
        )
        return self._write(
            "PATCH",
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways/{quote(gateway_id, safe='')}",
            body,
            VoiceTrunkGateway,
            options,
        )

    def delete(
        self,
        trunk_id: str,
        gateway_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Remove an inbound gateway. Removing the last gateway stops new inbound delivery through the trunk. Requires inbound calling enabled.

        ```python
        client.voice.trunks.gateways.delete("TRUNK_ID", "GATEWAY_ID")
        ```
        """
        self._delete(f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways/{quote(gateway_id, safe='')}", options)


class AsyncVoiceTrunksGateways(AsyncResource):
    async def list(
        self,
        trunk_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGatewayList:
        """Read the complete inbound gateway list for a trunk, ordered by priority. Available even when inbound calling is disabled.

        ```python
        gateways = await client.voice.trunks.gateways.list("TRUNK_ID")
        for gateway in gateways.data:
            print(gateway.id, gateway.priority)
        ```
        """
        return await self._get(
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways",
            {},
            VoiceTrunkGatewayList,
            options,
        )

    async def get(
        self,
        trunk_id: str,
        gateway_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGateway:
        """Read one inbound gateway and its SIP address, priority, and number formats. The gateway must belong to the specified trunk in this workspace.

        ```python
        gateway = await client.voice.trunks.gateways.get("TRUNK_ID", "GATEWAY_ID")
        print(gateway.id, gateway.priority)
        ```
        """
        return await self._get(
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways/{quote(gateway_id, safe='')}",
            {},
            VoiceTrunkGateway,
            options,
        )

    async def create(
        self,
        trunk_id: str,
        *,
        sip_uri: str,
        priority: int,
        origination_format: str | None = None,
        destination_format: str | None = None,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGateway:
        """Add an address for inbound call delivery. Enable inbound calling on the trunk first; a trunk supports up to 20 gateways.

        ```python
        gateway = await client.voice.trunks.gateways.create(
            "TRUNK_ID",
            sip_uri="sip:pbx.example.com:5060",
            priority=0,
            destination_format="1234#{number}",
        )
        print(gateway.id, gateway.priority)
        ```
        """
        body = to_wire(
            VoiceTrunkGatewayCreate,
            {
                "sip_uri": sip_uri,
                "priority": priority,
                "origination_format": origination_format,
                "destination_format": destination_format,
            },
        )
        return await self._write(
            "POST",
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways",
            body,
            VoiceTrunkGateway,
            options,
        )

    async def update(
        self,
        trunk_id: str,
        gateway_id: str,
        *,
        sip_uri: str | None = None,
        priority: int | None = None,
        origination_format: str | None = None,
        destination_format: str | None = None,
        options: RequestOptions | None = None,
    ) -> VoiceTrunkGateway:
        """Change an inbound gateway address, priority, or number formats. Omitted fields stay unchanged; an empty format restores E.164. Requires inbound calling enabled.

        ```python
        gateway = await client.voice.trunks.gateways.update("TRUNK_ID", "GATEWAY_ID", priority=10)
        print(gateway.id, gateway.priority)
        ```
        """
        body = to_wire(
            VoiceTrunkGatewayUpdate,
            {
                "sip_uri": sip_uri,
                "priority": priority,
                "origination_format": origination_format,
                "destination_format": destination_format,
            },
        )
        return await self._write(
            "PATCH",
            f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways/{quote(gateway_id, safe='')}",
            body,
            VoiceTrunkGateway,
            options,
        )

    async def delete(
        self,
        trunk_id: str,
        gateway_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """Remove an inbound gateway. Removing the last gateway stops new inbound delivery through the trunk. Requires inbound calling enabled.

        ```python
        await client.voice.trunks.gateways.delete("TRUNK_ID", "GATEWAY_ID")
        ```
        """
        await self._delete(f"/v1/voice/trunks/{quote(trunk_id, safe='')}/gateways/{quote(gateway_id, safe='')}", options)
