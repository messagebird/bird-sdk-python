# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict

from bird._generated import (
    EmailInboxInsightsIndustryBenchmark,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class _EmailInboxInsightsBenchmarksIndustryParamsRequired(TypedDict):
    sending_domain: str


class EmailInboxInsightsBenchmarksIndustryParams(_EmailInboxInsightsBenchmarksIndustryParamsRequired, total=False):
    """Query params for ``client.email.inbox_insights.benchmarks.industry``. Keys on the base class are required."""

    pass


class EmailInboxInsightsBenchmarks(Resource):
    def industry(
        self,
        *,
        sending_domain: str | None = None,
        options: RequestOptions | None = None,
    ) -> EmailInboxInsightsIndustryBenchmark:
        """Read the industry placement benchmark for a verified domain owned by the workspace. Compare its median with the separate placement report; the benchmark uses default weighting rather than the domain’s audience mix. No date window or previous-period comparison is accepted. A no_data status can mean the industry is unknown or has too few measured senders. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        sending_domain = None
        for domain in client.email.inbox_insights.domains.list(search="mail.example.com"):
            if domain.domain == "mail.example.com":
                sending_domain = domain.domain
                break
        if sending_domain is None:
            raise ValueError("Verify mail.example.com in this workspace first")
        report = client.email.inbox_insights.benchmarks.industry(sending_domain=sending_domain)
        print(report.model_dump_json())
        ```
        """
        return self._get(
            "/v1/email/inbox-insights/benchmarks/industry",
            {
                "sending_domain": sending_domain,
            },
            EmailInboxInsightsIndustryBenchmark,
            options,
        )


class AsyncEmailInboxInsightsBenchmarks(AsyncResource):
    async def industry(
        self,
        *,
        sending_domain: str | None = None,
        options: RequestOptions | None = None,
    ) -> EmailInboxInsightsIndustryBenchmark:
        """Read the industry placement benchmark for a verified domain owned by the workspace. Compare its median with the separate placement report; the benchmark uses default weighting rather than the domain’s audience mix. No date window or previous-period comparison is accepted. A no_data status can mean the industry is unknown or has too few measured senders. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        sending_domain = None
        async for domain in client.email.inbox_insights.domains.list(search="mail.example.com"):
            if domain.domain == "mail.example.com":
                sending_domain = domain.domain
                break
        if sending_domain is None:
            raise ValueError("Verify mail.example.com in this workspace first")
        report = await client.email.inbox_insights.benchmarks.industry(sending_domain=sending_domain)
        print(report.model_dump_json())
        ```
        """
        return await self._get(
            "/v1/email/inbox-insights/benchmarks/industry",
            {
                "sending_domain": sending_domain,
            },
            EmailInboxInsightsIndustryBenchmark,
            options,
        )
