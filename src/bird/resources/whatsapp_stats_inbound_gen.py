# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict

from bird._generated import (
    WhatsAppInboundStatsByPhoneNumberResponse,
    WhatsAppInboundStatsResponse,
    WhatsAppInboundStatsSummaryResponse,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class WhatsappStatsInboundSummaryParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.inbound.summary``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    compare: str


class WhatsappStatsInboundDailyParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.inbound.daily``. Every key is optional."""

    from_: str
    to: str
    timezone: str


class WhatsappStatsInboundHourlyParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.inbound.hourly``. Every key is optional."""

    from_: str
    to: str
    timezone: str


class WhatsappStatsInboundByPhoneNumberParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.stats.inbound.by_phone_number``. Every key is optional."""

    from_: str
    to: str
    timezone: str
    limit: int


class WhatsappStatsInbound(Resource):
    def summary(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        compare: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsSummaryResponse:
        """Aggregate count of the WhatsApp messages your workspace received in one window, with the delta against the window immediately before when `compare` is set to `previous_period`. Inbound is counted separately from outbound: this answers "how much are customers writing to us", where `whatsapp.stats.summary` answers "how much are we sending".

        ```python
        summary = client.whatsapp.stats.inbound.summary(from_="2026-05-01", to="2026-05-31")
        print(summary.received)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/inbound/summary",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "compare": compare,
            },
            WhatsAppInboundStatsSummaryResponse,
            options,
        )

    def daily(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsResponse:
        """Received WhatsApp message counts, one row per calendar day of the window, gap-filled so a silent day is a zero row. Days are calendar days in `timezone`, or UTC when it is omitted. Max window 365 days; `from` and `to` default to the trailing 30 days.

        ```python
        stats = client.whatsapp.stats.inbound.daily(from_="2026-05-01", to="2026-05-31")
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/inbound/daily",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            WhatsAppInboundStatsResponse,
            options,
        )

    def hourly(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsResponse:
        """Received WhatsApp message counts, one row per hour, gap-filled. A window spans at most 720 hours (30 days); for anything longer use `whatsapp.stats.inbound.daily`. `from` and `to` are optional RFC 3339 instants.

        ```python
        stats = client.whatsapp.stats.inbound.hourly(
            from_="2026-05-30T00:00:00Z", to="2026-05-31T00:00:00Z"
        )
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/inbound/hourly",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            WhatsAppInboundStatsResponse,
            options,
        )

    def by_phone_number(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsByPhoneNumberResponse:
        """Received WhatsApp message counts grouped by the business number that received them, ranked by volume. Use it to see which of your numbers customers actually write to. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = client.whatsapp.stats.inbound.by_phone_number(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            print(row.phone_number, row.received)
        ```
        """
        return self._get(
            "/v1/whatsapp/stats/inbound/phone-numbers",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppInboundStatsByPhoneNumberResponse,
            options,
        )


class AsyncWhatsappStatsInbound(AsyncResource):
    async def summary(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        compare: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsSummaryResponse:
        """Aggregate count of the WhatsApp messages your workspace received in one window, with the delta against the window immediately before when `compare` is set to `previous_period`. Inbound is counted separately from outbound: this answers "how much are customers writing to us", where `whatsapp.stats.summary` answers "how much are we sending".

        ```python
        summary = await client.whatsapp.stats.inbound.summary(from_="2026-05-01", to="2026-05-31")
        print(summary.received)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/inbound/summary",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "compare": compare,
            },
            WhatsAppInboundStatsSummaryResponse,
            options,
        )

    async def daily(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsResponse:
        """Received WhatsApp message counts, one row per calendar day of the window, gap-filled so a silent day is a zero row. Days are calendar days in `timezone`, or UTC when it is omitted. Max window 365 days; `from` and `to` default to the trailing 30 days.

        ```python
        stats = await client.whatsapp.stats.inbound.daily(from_="2026-05-01", to="2026-05-31")
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/inbound/daily",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            WhatsAppInboundStatsResponse,
            options,
        )

    async def hourly(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsResponse:
        """Received WhatsApp message counts, one row per hour, gap-filled. A window spans at most 720 hours (30 days); for anything longer use `whatsapp.stats.inbound.daily`. `from` and `to` are optional RFC 3339 instants.

        ```python
        stats = await client.whatsapp.stats.inbound.hourly(
            from_="2026-05-30T00:00:00Z", to="2026-05-31T00:00:00Z"
        )
        for point in stats.data or []:
            print(point.bucket, point.received)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/inbound/hourly",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
            },
            WhatsAppInboundStatsResponse,
            options,
        )

    async def by_phone_number(
        self,
        *,
        from_: str | None = None,
        to: str | None = None,
        timezone: str | None = None,
        limit: int | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppInboundStatsByPhoneNumberResponse:
        """Received WhatsApp message counts grouped by the business number that received them, ranked by volume. Use it to see which of your numbers customers actually write to. Cap the rows with `limit` (default 50, max 200).

        ```python
        stats = await client.whatsapp.stats.inbound.by_phone_number(from_="2026-05-01", to="2026-05-31")
        for row in stats.data or []:
            print(row.phone_number, row.received)
        ```
        """
        return await self._get(
            "/v1/whatsapp/stats/inbound/phone-numbers",
            {
                "from": from_,
                "to": to,
                "timezone": timezone,
                "limit": limit,
            },
            WhatsAppInboundStatsByPhoneNumberResponse,
            options,
        )
