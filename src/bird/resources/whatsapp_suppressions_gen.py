# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppSuppression,
    WhatsAppSuppressionCreate,
)
from bird._models import to_wire
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class WhatsappSuppressionsListParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.suppressions.list``. Every key is optional."""

    address: str
    reason: str
    limit: int
    starting_after: str
    ending_before: str


class _WhatsappSuppressionsAddRequired(TypedDict):
    address: str


class WhatsappSuppressionsAddParams(_WhatsappSuppressionsAddRequired, total=False):
    """Params for ``client.whatsapp.suppressions.add``. ``address`` is required."""

    waba: str


class WhatsappSuppressions(Resource):
    def list(
        self,
        *,
        address: str | None = None,
        reason: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[WhatsAppSuppression]:
        """List the workspace's recorded WhatsApp suppression addresses as a cursor page.

        ```python
        # address is a prefix, so a partial value matches every address under it.
        suppressions = client.whatsapp.suppressions.list(address="+1555")
        for suppression in suppressions.data or []:
            print(suppression.address, suppression.waba or "every account")
        ```
        """
        query = {
            "address": address,
            "reason": reason,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/whatsapp/suppressions", query, WhatsAppSuppression, options)

    def get(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppSuppression:
        """Read one WhatsApp suppression by ID, including one that has already ended. An ended record reports when it ended and what ended it. To find a suppression by address rather than by ID, use `whatsapp.suppressions.list` with its address filter.

        ```python
        # Resolves a record that has already ended, which the list leaves out.
        suppression = client.whatsapp.suppressions.get("was_01krdgeqcxet5s7t44vh8rt9mg")
        print(suppression.reason, suppression.ended_at or "still in force")
        ```
        """
        return self._get(
            f"/v1/whatsapp/suppressions/{quote(suppression_id, safe='')}",
            {},
            WhatsAppSuppression,
            options,
        )

    def add(
        self,
        *,
        address: str,
        waba: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppSuppression:
        """Record a WhatsApp address on the workspace suppression list.

        ```python
        # Omit waba to block the address for the whole workspace, whichever account
        # sends. With it, your other accounts keep reaching them, and the same
        # address for two accounts is two records.
        suppression = client.whatsapp.suppressions.add(
            address="+15550001234",
            waba="102290129340398",
        )
        print(suppression.id, suppression.applies_to)
        ```
        """
        body = to_wire(
            WhatsAppSuppressionCreate,
            {
                "address": address,
                "waba": waba,
            },
        )
        return self._write(
            "POST",
            "/v1/whatsapp/suppressions",
            body,
            WhatsAppSuppression,
            options,
        )

    def remove(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """End one WhatsApp suppression by ID. The record is kept and stays readable. Only suppressions with reason `manual` can be ended; a recipient's own opt-out is refused.

        ```python
        # Only a manual suppression can be ended; a recipient's own opt-out is
        # theirs to reverse. The record is kept and still reads back by id.
        client.whatsapp.suppressions.remove("was_01krdgeqcxet5s7t44vh8rt9mg")
        ```
        """
        self._delete(f"/v1/whatsapp/suppressions/{quote(suppression_id, safe='')}", options)


class AsyncWhatsappSuppressions(AsyncResource):
    def list(
        self,
        *,
        address: str | None = None,
        reason: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[WhatsAppSuppression]:
        """List the workspace's recorded WhatsApp suppression addresses as a cursor page.

        ```python
        # address is a prefix, so a partial value matches every address under it.
        suppressions = await client.whatsapp.suppressions.list(address="+1555")
        for suppression in suppressions.data or []:
            print(suppression.address, suppression.waba or "every account")
        ```
        """
        query = {
            "address": address,
            "reason": reason,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/whatsapp/suppressions", query, WhatsAppSuppression, options)

    async def get(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppSuppression:
        """Read one WhatsApp suppression by ID, including one that has already ended. An ended record reports when it ended and what ended it. To find a suppression by address rather than by ID, use `whatsapp.suppressions.list` with its address filter.

        ```python
        # Resolves a record that has already ended, which the list leaves out.
        suppression = await client.whatsapp.suppressions.get("was_01krdgeqcxet5s7t44vh8rt9mg")
        print(suppression.reason, suppression.ended_at or "still in force")
        ```
        """
        return await self._get(
            f"/v1/whatsapp/suppressions/{quote(suppression_id, safe='')}",
            {},
            WhatsAppSuppression,
            options,
        )

    async def add(
        self,
        *,
        address: str,
        waba: str | None = None,
        options: RequestOptions | None = None,
    ) -> WhatsAppSuppression:
        """Record a WhatsApp address on the workspace suppression list.

        ```python
        # Omit waba to block the address for the whole workspace, whichever account
        # sends. With it, your other accounts keep reaching them, and the same
        # address for two accounts is two records.
        suppression = await client.whatsapp.suppressions.add(
            address="+15550001234",
            waba="102290129340398",
        )
        print(suppression.id, suppression.applies_to)
        ```
        """
        body = to_wire(
            WhatsAppSuppressionCreate,
            {
                "address": address,
                "waba": waba,
            },
        )
        return await self._write(
            "POST",
            "/v1/whatsapp/suppressions",
            body,
            WhatsAppSuppression,
            options,
        )

    async def remove(
        self,
        suppression_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> None:
        """End one WhatsApp suppression by ID. The record is kept and stays readable. Only suppressions with reason `manual` can be ended; a recipient's own opt-out is refused.

        ```python
        # Only a manual suppression can be ended; a recipient's own opt-out is
        # theirs to reverse. The record is kept and still reads back by id.
        await client.whatsapp.suppressions.remove("was_01krdgeqcxet5s7t44vh8rt9mg")
        ```
        """
        await self._delete(f"/v1/whatsapp/suppressions/{quote(suppression_id, safe='')}", options)
