# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from urllib.parse import quote

from bird._generated import (
    WhatsAppGroupInviteLink,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class WhatsappGroupsInviteLink(Resource):
    def rotate(
        self,
        group_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupInviteLink:
        """Issue a new invite link for the group and invalidate every link issued before. Anyone holding an old link can no longer join. The group's current link is on the group itself, so read it rather than rotating to fetch it. Answers 200: the new link comes back on the call itself.

        ```python
        link = client.whatsapp.groups.invite_link.rotate("wag_01krdgeqcxet5s7t44vh8rt9mg")
        print(link.invite_link)  # every earlier link has stopped working
        ```
        """
        return self._action(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/invite-link/rotate",
            WhatsAppGroupInviteLink,
            options,
        )


class AsyncWhatsappGroupsInviteLink(AsyncResource):
    async def rotate(
        self,
        group_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppGroupInviteLink:
        """Issue a new invite link for the group and invalidate every link issued before. Anyone holding an old link can no longer join. The group's current link is on the group itself, so read it rather than rotating to fetch it. Answers 200: the new link comes back on the call itself.

        ```python
        link = await client.whatsapp.groups.invite_link.rotate("wag_01krdgeqcxet5s7t44vh8rt9mg")
        print(link.invite_link)  # every earlier link has stopped working
        ```
        """
        return await self._action(
            "POST",
            f"/v1/whatsapp/groups/{quote(group_id, safe='')}/invite-link/rotate",
            WhatsAppGroupInviteLink,
            options,
        )
