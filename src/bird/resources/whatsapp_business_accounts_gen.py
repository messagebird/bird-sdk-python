# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    WhatsAppBusinessAccount,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class WhatsappBusinessAccountsListParams(TypedDict, total=False):
    """Query params for ``client.whatsapp.business_accounts.list``. Every key is optional."""

    sort: str
    order: str
    limit: int
    starting_after: str
    ending_before: str


class WhatsappBusinessAccounts(Resource):
    def list(
        self,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[WhatsAppBusinessAccount]:
        """List the WhatsApp Business Accounts the workspace has connected, as a cursor page, with the state WhatsApp reports for each. The `waba` a returned account carries is the same value that ties a number to its account, and the one `whatsapp.numbers.list`'s `waba` filter takes.

        ```python
        for account in client.whatsapp.business_accounts.list():
            print(account.id, account.name, account.status)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, "/v1/whatsapp/business-accounts", query, WhatsAppBusinessAccount, options)

    def get(
        self,
        business_account_ref: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppBusinessAccount:
        """Read one connected WhatsApp Business Account by its `id` (`waa_` prefix) or the ID WhatsApp reports in `waba`, with the state WhatsApp reports for the account.

        ```python
        account = client.whatsapp.business_accounts.get("waa_01krdgeqcxet5s7t44vh8rt9mg")
        print(account.account_review_status, account.business_verification_status)
        ```
        """
        return self._get(
            f"/v1/whatsapp/business-accounts/{quote(business_account_ref, safe='')}",
            {},
            WhatsAppBusinessAccount,
            options,
        )


class AsyncWhatsappBusinessAccounts(AsyncResource):
    def list(
        self,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[WhatsAppBusinessAccount]:
        """List the WhatsApp Business Accounts the workspace has connected, as a cursor page, with the state WhatsApp reports for each. The `waba` a returned account carries is the same value that ties a number to its account, and the one `whatsapp.numbers.list`'s `waba` filter takes.

        ```python
        async for account in client.whatsapp.business_accounts.list():
            print(account.id, account.name, account.status)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, "/v1/whatsapp/business-accounts", query, WhatsAppBusinessAccount, options)

    async def get(
        self,
        business_account_ref: str,
        *,
        options: RequestOptions | None = None,
    ) -> WhatsAppBusinessAccount:
        """Read one connected WhatsApp Business Account by its `id` (`waa_` prefix) or the ID WhatsApp reports in `waba`, with the state WhatsApp reports for the account.

        ```python
        account = await client.whatsapp.business_accounts.get("waa_01krdgeqcxet5s7t44vh8rt9mg")
        print(account.account_review_status, account.business_verification_status)
        ```
        """
        return await self._get(
            f"/v1/whatsapp/business-accounts/{quote(business_account_ref, safe='')}",
            {},
            WhatsAppBusinessAccount,
            options,
        )
