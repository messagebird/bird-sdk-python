# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict

from bird._generated import (
    EmailCompetitiveBrandSearchResults,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions


class _EmailCompetitiveBrandsSearchParamsRequired(TypedDict):
    q: str


class EmailCompetitiveBrandsSearchParams(_EmailCompetitiveBrandsSearchParamsRequired, total=False):
    """Query params for ``client.email.competitive.brands.search``. Keys on the base class are required."""

    pass


class EmailCompetitiveBrands(Resource):
    def search(
        self,
        *,
        q: str | None = None,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveBrandSearchResults:
        """Search by brand name or sending domain. Each match returns the panel's `brand_id`, which is different from the `watchlist_brand_id` returned by `email.competitive.watchlist.get`. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        report = client.email.competitive.brands.search(q="Everlane")
        print(report.model_dump_json())
        ```
        """
        return self._get(
            "/v1/email/competitive/brands/search",
            {
                "q": q,
            },
            EmailCompetitiveBrandSearchResults,
            options,
        )


class AsyncEmailCompetitiveBrands(AsyncResource):
    async def search(
        self,
        *,
        q: str | None = None,
        options: RequestOptions | None = None,
    ) -> EmailCompetitiveBrandSearchResults:
        """Search by brand name or sending domain. Each match returns the panel's `brand_id`, which is different from the `watchlist_brand_id` returned by `email.competitive.watchlist.get`. API-key calls require Insights preview access for your organization.

        ```python
        # Requires Insights preview access for the organization.
        report = await client.email.competitive.brands.search(q="Everlane")
        print(report.model_dump_json())
        ```
        """
        return await self._get(
            "/v1/email/competitive/brands/search",
            {
                "q": q,
            },
            EmailCompetitiveBrandSearchResults,
            options,
        )
