# Code generated by surface-gen; DO NOT EDIT.

from __future__ import annotations

from typing import TypedDict
from urllib.parse import quote

from bird._generated import (
    SMSTemplateVersion,
    SMSTemplateVersionSummary,
)
from bird._resource import AsyncResource, Resource
from bird._types import RequestOptions
from bird.pagination import AsyncPage, SyncPage


class SmsTemplatesVersionsListParams(TypedDict, total=False):
    """Query params for ``client.sms_templates.versions.list``. Every key is optional."""

    sort: str
    order: str
    limit: int
    starting_after: str
    ending_before: str


class SmsTemplatesVersionsBase(Resource):
    def list(
        self,
        template_ref: str,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> SyncPage[SMSTemplateVersionSummary]:
        """List a template's permanent draft and immutable published versions, newest first. Built-in templates expose their current catalogue content as one synthetic published version.

        ```python
        for version in client.sms_templates.versions.list("bird_otp_verification"):
            print(version.id, version.version_number)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return SyncPage(self._client, f"/v1/sms/templates/{quote(template_ref, safe='')}/versions", query, SMSTemplateVersionSummary, options)

    def get(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSTemplateVersion:
        """Read one draft or published SMS template version, including its variables and text in every language. Built-in templates expose their current catalogue content through a synthetic published version.

        ```python
        version = client.sms_templates.versions.get(
            "bird_otp_verification", "smv_01ky4x8e4genzb7way45txfkm1"
        )
        print(version.id, list(version.languages))
        ```
        """
        return self._get(
            f"/v1/sms/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}",
            {},
            SMSTemplateVersion,
            options,
        )


class AsyncSmsTemplatesVersionsBase(AsyncResource):
    def list(
        self,
        template_ref: str,
        *,
        sort: str | None = None,
        order: str | None = None,
        limit: int | None = None,
        starting_after: str | None = None,
        ending_before: str | None = None,
        options: RequestOptions | None = None,
    ) -> AsyncPage[SMSTemplateVersionSummary]:
        """List a template's permanent draft and immutable published versions, newest first. Built-in templates expose their current catalogue content as one synthetic published version.

        ```python
        async for version in client.sms_templates.versions.list("bird_otp_verification"):
            print(version.id, version.version_number)
        ```
        """
        query = {
            "sort": sort,
            "order": order,
            "limit": limit,
            "starting_after": starting_after,
            "ending_before": ending_before,
        }
        return AsyncPage(self._client, f"/v1/sms/templates/{quote(template_ref, safe='')}/versions", query, SMSTemplateVersionSummary, options)

    async def get(
        self,
        template_ref: str,
        version_id: str,
        *,
        options: RequestOptions | None = None,
    ) -> SMSTemplateVersion:
        """Read one draft or published SMS template version, including its variables and text in every language. Built-in templates expose their current catalogue content through a synthetic published version.

        ```python
        version = await client.sms_templates.versions.get(
            "bird_otp_verification", "smv_01ky4x8e4genzb7way45txfkm1"
        )
        print(version.id, list(version.languages))
        ```
        """
        return await self._get(
            f"/v1/sms/templates/{quote(template_ref, safe='')}/versions/{quote(version_id, safe='')}",
            {},
            SMSTemplateVersion,
            options,
        )
