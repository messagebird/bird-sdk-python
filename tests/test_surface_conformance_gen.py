# Code generated by surface-gen; DO NOT EDIT.
# Every catalogued sdk method must exist and be callable on the client.
# Generated from backend/openapi/surfaces.ir.json.
from bird import Bird


def test_surface_conformance() -> None:
    client = Bird(api_key="bk_eu1_surfaceconformance")
    assert callable(client.email.send)
    assert callable(client.email.send_batch)
    assert callable(client.email.get)
    assert callable(client.email.list)
    assert callable(client.email.cancel)
    assert callable(client.email.stats.summary)
    assert callable(client.email.stats.daily)
    assert callable(client.email.stats.hourly)
    assert callable(client.email.stats.by_tag)
    assert callable(client.email.stats.by_category)
    assert callable(client.email.stats.by_sending_ip)
    assert callable(client.email.stats.by_sending_domain)
    assert callable(client.email.stats.by_recipient_domain)
    assert callable(client.email.stats.by_mailbox_provider)
    assert callable(client.email.stats.by_mailbox_provider_region)
    assert callable(client.email.stats.by_template)
    assert callable(client.email.stats.by_location)
    assert callable(client.email.stats.by_client)
    assert callable(client.email.stats.by_bounce_code)
    assert callable(client.email.stats.by_complaint_type)
    assert callable(client.email.stats.by_broadcast)
    assert callable(client.sms.send)
    assert callable(client.sms.send_batch)
    assert callable(client.sms.get)
    assert callable(client.sms.list)
    assert callable(client.sms_templates.list)
    assert callable(client.sms_templates.get)
    assert callable(client.sms.list_events)
    assert callable(client.sms.stats.summary)
    assert callable(client.sms.stats.daily)
    assert callable(client.sms.stats.hourly)
    assert callable(client.sms.stats.by_country)
    assert callable(client.sms.stats.by_carrier)
    assert callable(client.sms.stats.by_category)
    assert callable(client.sms.stats.by_originator)
    assert callable(client.sms.stats.by_status)
    assert callable(client.sms.stats.by_error_code)
    assert callable(client.sms.stats.by_tag)
    assert callable(client.sms.stats.inbound.summary)
    assert callable(client.sms.stats.inbound.daily)
    assert callable(client.sms.stats.inbound.hourly)
    assert callable(client.sms.stats.inbound.by_country)
    assert callable(client.sms.stats.inbound.by_operator)
    assert callable(client.sms.stats.inbound.by_number)
    assert callable(client.sms_suppressions.list)
    assert callable(client.sms_suppressions.get)
    assert callable(client.sms_suppressions.add)
    assert callable(client.sms_suppressions.remove)
    assert callable(client.sms_keyword_rules.list)
    assert callable(client.sms_keyword_rules.get)
    assert callable(client.sms_keyword_rules.create)
    assert callable(client.sms_keyword_rules.update)
    assert callable(client.sms_keyword_rules.delete)
    assert callable(client.whatsapp.send)
    assert callable(client.whatsapp.get)
    assert callable(client.whatsapp.list)
    assert callable(client.whatsapp.list_events)
    assert callable(client.voice.list)
    assert callable(client.voice.get)
    assert callable(client.verify.verifications.create)
    assert callable(client.verify.verifications.check)
    assert callable(client.verify.verifications.next_channel)
    assert callable(client.lookup.phone_number)
    assert callable(client.lookup.email)
    assert callable(client.numbers.list)
    assert callable(client.numbers.get)
    assert callable(client.numbers.release)
    assert callable(client.numbers.available.list)
    assert callable(client.numbers.available.get)
    assert callable(client.numbers.orders.create)
    assert callable(client.numbers.orders.list)
    assert callable(client.numbers.orders.get)
    assert callable(client.domains.list)
    assert callable(client.domains.get)
    assert callable(client.domains.create)
    assert callable(client.domains.verify)
    assert callable(client.domains.update)
    assert callable(client.domains.delete)
    assert callable(client.webhooks.unwrap)
    assert callable(client.contacts.list)
    assert callable(client.contacts.get)
    assert callable(client.contacts.create)
    assert callable(client.contacts.update)
    assert callable(client.contacts.delete)
    assert callable(client.contacts.batch)
    assert callable(client.contact_properties.list)
    assert callable(client.contact_properties.get)
    assert callable(client.contact_properties.create)
    assert callable(client.contact_properties.update)
    assert callable(client.contact_properties.archive)
    assert callable(client.contact_properties.unarchive)
    assert callable(client.audiences.list)
    assert callable(client.audiences.get)
    assert callable(client.audiences.create)
    assert callable(client.audiences.update)
    assert callable(client.audiences.delete)
    assert callable(client.audiences.list_contacts)
    assert callable(client.audiences.add_contacts)
    assert callable(client.audiences.remove_contacts)
    assert callable(client.audiences.remove_contact)
    assert callable(client.email.mailboxes.list)
    assert callable(client.email.mailboxes.create)
    assert callable(client.email.mailboxes.get)
    assert callable(client.email.mailboxes.update)
    assert callable(client.email.mailboxes.delete)
    assert callable(client.email.mailboxes.restore)
    assert callable(client.email.mailboxes.resume)
    assert callable(client.email.mailboxes.stats)
    assert callable(client.email.mailboxes.messages.create)
    assert callable(client.email.mailboxes.labels)
    assert callable(client.email.mailboxes.receive_rules.list)
    assert callable(client.email.mailboxes.receive_rules.create)
    assert callable(client.email.mailboxes.receive_rules.delete)
    assert callable(client.email.threads.list)
    assert callable(client.email.threads.get)
    assert callable(client.email.threads.update)
    assert callable(client.email.threads.delete)
    assert callable(client.email.threads.messages.list)
    assert callable(client.email.threads.messages.get)
    assert callable(client.email.threads.messages.body)
    assert callable(client.email.threads.messages.reply)
    assert callable(client.email.threads.messages.attachments)
    assert callable(client.realtime.publish)
    assert callable(client.realtime.publish_batch)
    assert callable(client.realtime.channels.list)
    assert callable(client.realtime.channels.get)
    assert callable(client.realtime.channels.members)
    assert callable(client.realtime.members.send)
    assert callable(client.realtime.members.disconnect)
